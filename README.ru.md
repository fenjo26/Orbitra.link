# Orbitra v1.2.0 Tracker

**🌐 Язык: [English](README.md) | Русский**

![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)
![PHP Version](https://img.shields.io/badge/PHP-8.0+-777BB4?logo=php)
![React](https://img.shields.io/badge/React-19-61DAFB?logo=react)
![Vite](https://img.shields.io/badge/Vite-7-646CFF?logo=vite)
![SQLite](https://img.shields.io/badge/SQLite-3-003B57?logo=sqlite)
![Status](https://img.shields.io/badge/Status-Production_Ready-brightgreen)

Orbitra — современная система управления трафиком и трекинга конверсий. Более простая и быстрая альтернатива Keitaro Tracker с сохранением полной совместимости по API и функциям.

## 🆕 Новое в v1.2.0

### Исправлено

- **📱 Панель на телефоне была виртуальным десктопом 980px** — во фронтенде вообще не было meta viewport: мобильный браузер рендерил виртуальный десктоп, и ни один `lg:`-брейкпоинт из кода не срабатывал; один тег (`width=device-width, viewport-fit=cover`) плюс мобильная вёрстка ниже делают панель по-настоящему пригодной для телефона
- **🧭 Меню «Создать поток» было мертво на тачскринах** — открывалось по hover; теперь по клику, тач-цели 44px
- **🛟 router.php съедал реальные файлы в корне** — `/sw.js` (и любой реальный файл корня домена) попадал в ветку кампаний-алиасов и умирал пустой 200; guard `file_exists` теперь повторяет Apache `RewriteCond !-f`

### Добавлено

- **📱 Мобильная вёрстка** — пять списков (Кампании, Офферы, Лендинги, Конверсии, Логи) ниже `lg` переключаются на карточные строки через общий компонент MobileCards; редактор кампании — в одну колонку; пагинация и тулбары отчётов переносятся по строкам
- **📲 PWA** — манифест отдаёт сам admin.php, поэтому `start_url` всегда совпадает с реальным входом (/admin.php или секретный путь — статический файл такое не покрыл бы никогда); иконки 192/512 + maskable + apple-touch; устанавливается на домашний экран
- **⚙️ Service worker** — шелл панели network-first (офлайн-фолбэк только по делу), хешированные ассеты stale-while-revalidate; новая сборка подменяется при перезагрузке через тост «доступно обновление» — никогда посреди сессии
- **#️⃣ Хешированные имена сборки** — vite эмитит `index-[hash].js|css`, admin.php резолвит их через `.vite/manifest.json` (заодно лечит устаревший шелл после частичного деплоя); кэш-бастер `?v=filemtime` и совет «сделайте hard-reload после обновления» больше не нужны
- **🔄 Автооптимизатор ротации** — переключите список лендингов/офферов потока в *Auto* и выберите метрику (подтверждённые продажи, CR, EPV, EPC, ROI); крон каждые 5 минут пересчитывает веса тем же движком метрик, что и отчёты, по скользящему окну 7 дней (safe-клики исключены, боты учтены), ходы лимитированы (потолок 70%, пол 5%, ≤20пп за прогон), каждое решение пишется в аудит `stream_rotation_log`, а сохранение кампании возвращает крону принадлежащие ему веса вместо воскрешения устаревших

### Предыдущие релизы (v1.1.11)

### Исправлено

- **🩹 Цифры клоака расходились между экранами** — список кампаний с датой считал и safe-клики, а «Лендинги»/«Офферы» и дашборд их исключали (M+N рядом с M); все четыре поверхности теперь спрашивают один общий хелпер, а `COALESCE(is_safe_page, 0)` сохраняет в отчётах строки до-обсервабельности (NULL = трафик money-стороны) вместо их молчаливого исчезновения
- **🕐 Окно панели клоака vs таймзона отчёта** — `cloak_summary` округлял дни по UTC, тогда как соседний список кампаний делил по дням в таймзоне отчёта; панель теперь работает в той же таймзоне (переопределяется через `?timezone=`) и возвращает точное окно, по которому считала, — две цифры на соседних экранах больше не могут молча означать разные периоды
- **🛟 Панель диагностики переживает деградировавшую базу** — отсутствие `cloak_suppressed_stats` больше не роняет панель клоака (откат к 0, таблица восстанавливается self-heal DDL ниже)

### Добавлено

- **🔍 Доказательство (evidence) в каждой причине клоака** — коды причин теперь несут факт-триггер: `crawler_or_tool_ua:curl/` (сигнатура), `iprange_datacenter:52.95.0.0/8` (совпавший CIDR), `bot_isp:hetzner` (совпавшая запись), `geo_country:US`, `ip2proxy_high_fraud:87`; видно в логе кликов и деталях клика, а агрегация срезает evidence — пороги и группировка остаются по слою детекции
- **🔗 Панель клоака → глубокая ссылка в лог кликов** — карточка диагностики открывает лог преднастроенным фильтром (маршрут, окно 24ч, поток); модалка лога получает вкладки ALL/SAFE/MONEY плюс фильтры часов/потока, а в деталях клика — сетевые факты решения (ISP, ASN, тип прокси, вердикт/причины)
- **🛠️ Self-heal DDL для `cloak_suppressed_stats`** — базы, помеченные актуальной версией схемы ранней сборкой cloak-миграции, больше её не перезапускали и жили без таблицы (корень саппорт-кейсов «диагностика клоака пустая»); таблица идемпотентно пересоздаётся на каждом запуске
- **📋 Бот-ISP блэклист в формате Keitaro** — один провайдер на строку, сопоставление целой фразой (запятые/точки/апострофы — часть имени: «Amazon.com, Inc.»); родовые корпоративные суффиксы («Inc», «Ltd», «GmbH») и записи короче 3 символов игнорируются роутером и подсвечиваются предупреждением у textarea настроек (зеркальная пара PHP/JS); матчинг терпит точку на конце («ZSCALER, INC.» находит строку без точки)
- **⭐ Звезда Group By + последний применённый** — закрепите группировку звёздочкой, и она станет дефолтом для новых отчётов (затем — последний применённый вариант, затем country); кнопка «Отчёт» в Кампаниях следует за единственной отмеченной галочкой и открывает отчёт той кампании


### Предыдущие релизы (v1.1.10)

### Исправлено

- **🩹 КРИТИЧНО: таблица «Лендинги» рисовалась прочерками в v1.1.9** — рерайт передавал объект колонки в рендерер ячейки, все фиксированные колонки (ID, статус, имя, группа, тип, URL, Last Event) превращались в «-», а метрики съезжали на колонку влево; строки рендерятся полностью
- **🖱️ Drag-and-drop колонок не стартовал** — grip сидел внутри кнопки сортировки (нативный drag не начинается на интерактивном потомке), компонент шапки перемонтировался на каждом рендере и убивал drag подсветкой, а payload не выставлялся (Firefox вообще не начинал); исправлено в Кампаниях и Офферах, payload-фикс в отчётах
- **🎛️ Выпада́шки навбара за оверлеем отчёта** — слой навбара поднят над страничными оверлеями; модалки и мобильный drawer сохранили порядок

### Добавлено

- **🔌 Кнопки параметров ведутся из источника трафика** — «Параметры Facebook» и «Добавить все трекинг-параметры» строятся из `parameters_json` кампании (тот же контракт {alias, param, macro}, что на пути клика); пресет Direct-URL теперь мержится в существующий query вместо стирания ручных параметров (значения пользователя выигрывают, ключи не дублируются); до выбора источника — универсальные Facebook-дефолты с подсказкой
- **⚖️ «Разделить поровну» + живые бейджи доли** в списках офферов/лендингов потока — деление только по включенным (ровно тот набор, что крутит роутер), выключенные сохраняют вес; статичный «%» заменён бейджем живой доли (вес / сумма включенных, один знак, «—» на паузе)

**После обновления один раз сделайте hard-reload панели (Ctrl/Cmd+Shift+R).**

### Основное в v1.1.9

### Добавлено

- **🩹 Автолечение двойного `?` в URL** — ведущий `?` в поле «URL-параметры» Facebook Ads (или клоака, приклеившая параметры к ссылке, где `?` уже был) ломал первый routing-параметр и клик терялся; теперь лечение работает до резолва кампании во всех 3 точках входа, чинит повреждённое значение и восстанавливает все «проглоченные» ключи (`utm_placement` тоже захватывается)
- **📘 Кнопка «Параметры Facebook»** в редакторе кампаний — копирует чистую строку трекинг-параметров (без ведущего `?`) для Meta Ads Manager
- **➡️ Пресет «Добавить все трекинг-параметры»** для Direct-URL; неразрешённые `{макросы}` вычищаются из редирект-URL — литералы больше не уезжают в партнёрскую сеть
- **🔍 Наблюдаемость клоака** — каждое решение о маршрутизации сохраняется и видно в панели: вердикт + коды причин + ISP/ASN у каждого клика, колонки «Маршрут/Причина/Назначение» в Analytics → Clicks с фильтрами, счётчик подавленных хитов по дням («ноль кликов» больше не спрячет реальный трафик), клики Safe Page по умолчанию пишутся и исключаются из отчётов по флагу, предупреждения гео-безопасности (нет гео-базы → `geo_unknown` + настраиваемое действие) в редакторе и на странице «Гео-базы». Полное руководство: `docs/cloak-how-it-works.md`
- **📊 Полный паритет метрик на Лендингах/Офферах** — регистрации, депозиты, боты/прокси, выручка по статусам и семейство real-revenue считаются по-настоящему (раньше всегда 0, real_roi показывал фиктивные −100%); на обеих страницах — настраиваемая таблица метрик с пресетами и итогами

### Исправлено

- **🎛️ Ротация учитывает индивидуальные выключатели элементов** — выключенный оффер/лендинг внутри кастомной схемы больше не получает трафик; взвешенный выбор фильтрует отключённые элементы
- **🌍 Локали в полном паритете на всех 7 языках** (раньше новые ключи приезжали только на английском); плавающий виджет расширения запоминает позицию
- **После обновления один раз сделайте hard-reload панели (Ctrl/Cmd+Shift+R)** — имя файла `index.js` стабильное, браузеры кэшируют старую сборку

### Основное в v1.1.8

- **🎯 Фильтры по сущностям в Аналитике** — мультивыбор кампаний, офферов и лендингов в режимах Тренд и Когорта; дист пересобран (прошлая попытка не показывалась именно из-за него)

### Старее (v1.1.7)

- **🛟 Ассеты лендингов: редирект-луп в nginx (500)** — выпрямленная локация `/_internal_assets/`; фолбэк распознаёт сломанный конфиг до запуска `nginx_sync.php`

### Более старое (v1.1.6)

- **🔀 Выбор режима SSL в «Доменах»** — Let's Encrypt / Cloudflare / свой сертификат, 7 языков
- **🧰 Smoke-тесты install.sh + `cli/check_landings.php`**
- **🧩 Гарантированная загрузка ассетов лендингов/офферов** — относительные пути, referer-фолбэк по URL кампаний, PHP-фолбэк

## 🖥 Живое демо

Попробуйте панель целиком — без установки:

- **URL:** [https://demo.orbitra.link/admin.php](https://demo.orbitra.link/admin.php)
- **Логин:** `admin`
- **Пароль:** `password`

> Это общий демо-стенд — не храните на нём ничего важного, данные могут сбрасываться в любой момент.

## 🚀 Быстрая установка (Ubuntu 20.04 / 22.04 / 24.04)

Для автоматической установки на чистый Linux-сервер выполните:

```bash
wget -qO- https://raw.githubusercontent.com/fenjo26/Orbitra.link/main/install.sh | bash
```

Установщик автоматически:
- Скачивает исходный код с GitHub
- Устанавливает Nginx, PHP 8.0+ (FPM), SQLite3 и Node.js 20
- Собирает и разворачивает фронтенд на React/Vite
- Настраивает SSL-сертификат Let's Encrypt для вашего домена

## 🖥 Системные требования

Orbitra намеренно лёгкая — она работает на связке **PHP + SQLite** за Nginx, без тяжёлых фреймворков и **без отдельного сервера БД** (нет ClickHouse, Redis или MySQL). Благодаря этому ей нужно намного меньше RAM, чем трекерам на ClickHouse, и она комфортно помещается на самых дешёвых VPS.

### Базовая конфигурация (обязательная)

- **CPU:** 1 vCPU (x86_64)
- **RAM:** 1 ГБ (рекомендуется 2 ГБ)
- **Диск:** 20 ГБ SSD
- **ОС:** Ubuntu 20.04 / 22.04 / 24.04 или Debian 11 / 12
- Чистый сервер, без панели управления; доступ root/sudo

### Подбор ресурсов под трафик (ориентир)

| Кликов в день | RAM | CPU | Диск |
|---|---|---|---|
| до ~100 000 | 1–2 ГБ | 1–2 vCPU | 20 ГБ SSD |
| ~100 000 – 500 000 | 2–4 ГБ | 2 vCPU | 40 ГБ SSD |
| ~500 000 – 1 000 000 | 4–8 ГБ | 4 vCPU | 80 ГБ SSD |

> ✅ **Проверено на практике:** Orbitra хорошо работает на VPS **2 vCPU / 2 ГБ RAM / 20 ГБ SSD** (Ubuntu 24.04) на небольшом трафике — удобная и недорогая точка старта. Более высокие строки таблицы — запас на тяжёлый трафик, а не жёсткое требование.

**ПО (устанавливается автоматически через `install.sh`):** Nginx, PHP 8.0+ с FPM (`php-sqlite3`, `php-curl`, `php-mbstring`, `php-xml`, `php-zip`), SQLite 3, Node.js 20 (только для сборки), Certbot для SSL.

> 💡 **Почему требования ниже, чем у Keitaro?** Keitaro хранит клики в ClickHouse + Redis + MySQL, поэтому его требования к RAM растут очень быстро (до 64 ГБ при миллионах кликов в день). Orbitra держит всё в одном файле SQLite, поэтому узкое место — не RAM, а дисковый I/O и модель одного писателя SQLite. SQLite (в режиме WAL) уверенно тянет низкие и средние объёмы; для устойчивых **миллионов кликов в день** с тяжёлой аналитикой архитектурно лучше подходит трекер на колоночной БД, например Keitaro.

> 💡 **Про тариф на 1 ГБ:** самому трекеру нужно очень мало памяти, но установщик собирает фронтенд на сервере через Vite — это самый прожорливый по памяти шаг. На машине с 1 ГБ добавьте ~1–2 ГБ swap перед установкой (или собирайте фронтенд отдельно), чтобы сборке хватило памяти. Диск расходуется скромно — он растёт в основном за счёт логов кликов/конверсий в SQLite со временем.

## ✨ Ключевые возможности

### 1. **Полная совместимость с Keitaro**
- **Click API с токенами** — полная совместимость с существующими интеграционными скриптами
- **Импорт из Keitaro** — перенос кампаний, офферов, доменов и потоков с сохранением токенов
- **Совместимость по API** — работает с существующими постбеками и вебхуками

### 2. **Современная архитектура**
- **Бэкенд**: PHP 8.3+ без тяжёлых фреймворков (чистый код)
- **База данных**: SQLite 3 (один файл, автоматическое создание схемы)
- **Фронтенд**: React 19 + Vite 7 + Tailwind CSS 4
- **UI/UX**: современный дизайн с несколькими встроенными темами (светлая, тёмная, зелёная, неон) и кастомной палитрой

### 3. **Управление кампаниями**
- **6 моделей выплат**: CPC, CPuC, CPM, CPA, CPS, RevShare
- **30+ параметров**: keyword, sub_id_1...30, cost, creative_id и другие
- **Продвинутая логика потоков**: Intercept → Regular → Fallback с весами и позициями
- **Гибкая фильтрация**: GEO, устройство (единая таксономия Десктоп / Мобильные / Планшеты в трекере, Click API и отчётах), ОС, браузер, ISP, IP, язык, реферер
- **A/B-тесты**: встроенный сплит-тест с взвешенной ротацией
- **Play/Pause из панели** — одним кликом ставится на паузу внутренняя кампания (отключённая сразу перестаёт раздаваться) или реальный ad / адсет / кампания Facebook прямо из таблицы или строки отчёта, через Meta Marketing API
- **«Собирать клики» на уровне потока** — фолбэк и white-page потоки могут отдавать направление, не записывая клик: мусорный трафик перестаёт разъезжать CR и CPA

### 4. **Интеграции**
- **S2S-постбеки** — Server-to-Server постбеки от партнёрских сетей
- **Шаблоны партнёрок**: уровня платформ (Everflow, CAKE, HitPath, Affise, TUNE/HasOffers), а также сети Leadbit, M4Leads, Dr.Cash, AdCombo и другие
- **Шаблоны источников**: Facebook, Google, TikTok, Yandex, Taboola, Outbrain, Email и другие
- **Click API** — токены для работы с интеграционными скриптами
- **Импорт расходов Facebook** — дневной расход из Meta Marketing API, атрибуция к кликам по ad / adset / campaign ID и конвертация в валюту трекера ([документация](docs/facebook.md))
- **Импорт расходов TikTok Ads и Google Ads** — тот же конвейер атрибуции расхода для остальных крупных сетей
- **Внешний API расходов** — Dolphin и Fbtool заливают расход прямо в Orbitra через Keitaro-совместимый Admin API ([документация](docs/dolphin-fbtool.md))
- **Facebook Conversions API** — отправка конверсий в Meta с сервера, с дедупликацией против браузерного пикселя: события, которые режут блокировщики и iOS, всё равно доходят до оптимизатора
- **Расширение для Ads Manager** — оверлей в браузере: в таблицы Facebook Ads Manager инъектируются пилюли с реальными прибылью / ROI / CPA, а по клику — раскладка по сущности: история по дням, лендинги и офферы, точность доставки Pixel/CAPI (read-ключ создаётся автоматически, прав на другие сайты нет)
- **Cloudflare и Namecheap** — парковка DNS и SSL через API Cloudflare; покупка, парковка и импорт доменов через Namecheap, не выходя из панели
- **Агрегаторы выручки** — Affilka, ReferOn и generic S2S-API возвращают в отчёты реальную выручку по игрокам
- **Telegram-бот** — мониторинг и уведомления в реальном времени

### 5. **Аналитика и отчёты**
- **Дашборд** — сводная статистика по кликам, конверсиям и доходу
- **Тренды** — детальная аналитика с графиками по 8 метрикам
- **Отчёты по кампаниям** — группировка по любому параметру, сохраняемые шаблоны колонок и Keitaro-паритетный набор метрик (visits, LP CTR, лиды/сейлы/rejected/trash, Approve %, EPC/EPV, ROI/ROI(conf), CPV)
- **Когортный анализ** — как держатся кампании, запущенные в разные даты
- **Плотные таблицы** — компактные таблицы Кампаний / Лендингов / Офферов с закреплённой шапкой, зеброй, пагинацией и всегда видимой строкой TOTAL
- **Лог конверсий** — детальный лог конверсий с фильтрами
- **Симуляция трафика** — симуляция кликов для тестирования потоков

### 6. **Мультиязычность**
- **7 языков**: 🇬🇧 английский, 🇷🇺 русский, 🇺🇦 украинский, 🇪🇸 испанский, 🇨🇳 китайский (упрощённый), 🇫🇷 французский, 🇩🇪 немецкий
- **Полное покрытие i18n** — локализован каждый элемент интерфейса, 100% паритет ключей во всех локалях
- **Переключение языка** — в настройках профиля, без перезагрузки страницы

### 7. **Telegram-бот**
- **10+ команд**: `/stats`, `/campaigns`, `/top`, `/conversions` и другие
- **Уведомления**: моментальные уведомления о конверсиях
- **Ежедневная сводка**: автоматический отчёт по кампаниям
- **Мультиязычность**: бот говорит на всех 7 языках интерфейса (EN, RU, UK, ES, ZH, FR, DE) через `/lang`

### 8. **Управление доменами**
- **Группы доменов** — организация припаркованных доменов («FB Nutra», «TikTok Landers», …) с созданием групп прямо из модалки домена
- **Контролы на домен** — доступ к админ-панели (запрет = панель и API отвечают 404 на этом хосте, трекинг продолжает работать), HTTPS-only, прокси Cloudflare (SSL от edge CF, выпуск Let's Encrypt пропускается), индексация роботами
- **Индекс-страница и Catch 404** — отдача кампании на корне домена или перехват неизвестных путей
- **Метаданные** — регистратор, DNS-провайдер и ручной статус (Disabled = 404 на весь хост)
- **Массовое добавление с чисткой URL** — вставьте `https://track.example.com/` или список через запятую; HTTP(S), слеши и пробелы чистятся автоматически
- **Проверка DNS** — автоматическая проверка A-записи с кэшированием
- **Автоматический SSL** — Let's Encrypt через Certbot с повторами и проверкой полноты цепочки; нулевая конфигурация парковки сама пишет A-запись при подключённой интеграции Cloudflare или Namecheap

### 9. **Миграция из Keitaro**
- **Полный перенос данных**: кампании, офферы, домены, потоки, партнёрские сети, источники, лендинги
- **Сохранение токенов** — токены Click API переносятся для совместимости
- **Гайд внутри UI** — пошаговая инструкция по созданию бэкапа Keitaro
- **Режим предпросмотра** — предпросмотр перед реальным импортом

### 10. **Anti-Bot Challenge**
- **Проверка на человека для каждой кампании** — не даёт корпоративным email-краулерам и кликботам засорять статистику
- **reCAPTCHA v2** — классический чекбокс «Я не робот»
- **reCAPTCHA v3** — невидимая, на основе score с настраиваемым порогом
- **Кастомный код** — вставьте любой HTML/JS-виджет проверки
- **Чистая статистика** — клики логируются только после успешной проверки, поэтому боты не попадают в отчёты; состояние проверки подписывается (HMAC-SHA256) и истекает через 15 минут для защиты от replay

### 11. **Роли и безопасность**
- **RBAC-роли** — шаблоны одной кнопкой (Admin / Media Buyer / Video Editor / Developer / Custom) заполняют всю матрицу прав по ресурсам
- **Серверное маскирование финансов** — пользователям без `show_costs` / `show_revenue` / `show_payout` деньги обнуляются по всей панели: метрики, графики, кампании, офферы; save-гарды не дают замаскированной загрузке редактора затереть сохранённые суммы
- **Персональные API-ключи** — ключи `read` / `write` на пользователя для MCP, Admin API и расширения Ads Manager
- **Защита от ботов и клоакинг** — детект датацентров/VPN по ASN, эвристики UA, блэклисты бот-ISP, отдача safe-page подозрительным с контролем на уровне потока, что попадает в статистику

## 🤖 Интеграция с нейросетями (MCP)

Начиная с версии 0.9.5.0 к Orbitra можно подключить ИИ-ассистента (Claude Desktop и другие MCP-клиенты) через **MCP-сервер** в папке [`mcp/`](mcp/README.md). После подключения трекером можно управлять на обычном языке:

> «Как отработали кампании за последние 7 дней?»
> «Создай 10 кампаний под оффер №4 — по одной на GEO: US, CA, GB, DE, FR.»
> «Добавь домен track.example.com и повесь на его корень кампанию 12.»

- **31 инструмент**: чтение (метрики, кампании, конверсии, отчёты) и управление (создание/массовое создание/редактирование/удаление кампаний, офферов, доменов, источников, лендингов).
- **API-ключи с областями прав**: `read` (только аналитика) и `write` (управление). Генерируются в **Пользователи → API-ключи**.
- **Безопасно**: ключ уходит только на ваш адрес, read-ключи физически не могут менять данные.

Подробнее — [docs/mcp.md](docs/mcp.md) и [mcp/README.md](mcp/README.md).

## 📁 Структура проекта

```
Orbitra/
├── api.php                    # REST API (60+ эндпоинтов)
├── index.php                  # Основной трекер (обработка кликов)
├── admin.php                  # Точка входа админ-панели
├── postback.php               # Обработчик постбеков
├── click.php                  # Click API
├── telegram_bot.php           # Webhook-обработчик Telegram-бота
├── config.php                 # Конфигурация БД и миграции
├── database.sql               # Документация схемы БД
├── version.php                # Версия системы
├── router.php                 # Роутер встроенного PHP-сервера
├── install.sh                 # Автоустановщик
├── *_cron.php                 # Cron-задачи (агрегатор, backorder, проверка источников)
├── .htaccess                  # Правила rewrite для Apache
│
├── core/                      # Модули системы
│   ├── keitaro_import.php     # Импорт из Keitaro
│   ├── click_api.php          # Реализация Click API
│   ├── backorder.php          # Мониторинг доменов
│   └── SxGeo.php              # Гео IP-база
│
├── frontend/                  # Фронтенд на React + Vite
│   ├── src/
│   │   ├── App.jsx           # Главный компонент с роутингом
│   │   ├── main.jsx          # Точка входа
│   │   ├── components/       # 53 React-компонента
│   │   ├── contexts/         # LanguageContext (i18n)
│   │   └── locales/          # 7 языков, 100% паритет ключей
│   ├── package.json
│   ├── vite.config.js
│   └── index.html
│
├── mcp/                       # MCP-сервер для нейросетей (Node.js)
│   ├── src/index.js          # инструменты (чтение + управление)
│   └── README.md             # подключение Claude Desktop
│
├── docs/                      # Документация
│   ├── index.md              # Обзор документации
│   ├── architecture.md       # Архитектура и технологии
│   ├── features.md          # Описание функций
│   ├── api.md               # Документация REST API
│   ├── mcp.md               # MCP-сервер для нейросетей
│   ├── deployment.md        # Инструкции по деплою
│   └── keitaro-migration.md # Гайд по миграции из Keitaro
│
├── aggregator_engines/        # Движки агрегации статистики
├── cli/                       # CLI-утилиты
├── landings/                  # Загруженные лендинги
└── vendor/                    # Зависимости Composer
```

## 🚀 Быстрый старт для разработчиков

### Локальная разработка

```bash
# Клонировать репозиторий
git clone https://github.com/fenjo26/Orbitra.link.git
cd Orbitra

# Установить PHP-зависимости
composer install --no-dev

# Установить зависимости фронтенда
cd frontend
npm install
npm run dev  # Запустить dev-сервер (http://localhost:5173)
```

### Запуск бэкенда

```bash
# В корне проекта
php -S localhost:8080 router.php
```

### Продакшн-сборка фронтенда

```bash
cd frontend
npm run build
```

## 🔐 Настройка OAuth для Google Ads (1-Click)

Orbitra поддерживает бесшовное подключение Google Ads в один клик (как в Keitaro Tracker). Для включения этой функции настройте следующие переменные окружения на сервере:

### Серверная конфигурация

Добавьте переменные окружения в конфигурацию сервера (например, в `/etc/environment`, systemd service файл или `.env` файл):

```bash
# Google OAuth2 Client ID (веб-приложение)
ORBITRA_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# Google OAuth2 Client Secret
ORBITRA_GOOGLE_CLIENT_SECRET=your-client-secret-here

# Developer Token для Google Ads API
ORBITRA_GOOGLE_DEVELOPER_TOKEN=your-developer-token
```

### Как получить учетные данные API Google Ads

1. **Создайте проект в Google Cloud:**
   - Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
   - Создайте новый проект или выберите существующий
   - Включите **Google Ads API** в библиотеке API

2. **Создайте учетные данные OAuth 2.0:**
   - Перейдите в **APIs & Services → Учетные данные**
   - Нажмите **Создать учетные данные → OAuth 2.0 Client ID**
   - Тип приложения: **Веб-приложение**
   - Добавьте этот URI перенаправления (замените на ваш домен):
     ```
     https://your-domain.com/api.php?action=google_ads_oauth_callback
     ```
   - Скопируйте **Client ID** и **Client Secret**

3. **Получите Developer Token:**
   - Перейдите в [API Center Google Ads](https://ads.google.com/aw/apicenter)
   - Подайте заявку на Developer Token или скопируйте существующий
   - Вставьте токен в конфигурацию

### Работа пользователей

После настройки пользователи могут:
1. Нажать кнопку **"Войти через Google"** в **Интеграции → Расходы Google Ads**
2. Выбрать свой Google-аккаунт (все вошедшие Gmail-профили будут отображены)
3. Автоматически обнаружить все доступные аккаунты Google Ads (включая иерархию MCC)
4. Выбрать аккаунты для подключения и сохранить

### Резервный вариант: прямое подключение по токену

Если OAuth на уровне сервера не настроен, интерфейс автоматически показывает вкладку **"Прямое подключение"**, где пользователи могут ввести:
- Developer Token
- OAuth2 Client ID
- OAuth2 Client Secret
- OAuth2 Refresh Token
- Customer ID

## 🔐 Первый вход и настройка

В Orbitra **нет** аккаунта по умолчанию (`admin`/`admin`) — учётные данные администратора вы задаёте сами при первом запуске.

Когда вы впервые открываете админ-панель (`/admin.php`), система обнаруживает, что пользователей ещё нет, и запускает **мастер первичной настройки**. В нём вы создаёте своего администратора:

- **Имя пользователя** — минимум 3 символа
- **Пароль** — минимум 6 символов (с подтверждением)
- **Часовой пояс** и **язык интерфейса** (один из 7 языков)

После создания администратора мастер больше не появляется, и вы входите с заданными логином и паролем.

## 📚 Документация

Полная документация — в папке [docs/](docs/):

- **[Обзор](docs/index.md)** — навигация по документации
- **[Архитектура](docs/architecture.md)** — технологический стек и структура БД
- **[Функции](docs/features.md)** — детальное описание возможностей
- **[API](docs/api.md)** — документация REST API
- **[MCP](docs/mcp.md)** — подключение нейросетей к трекеру
- **[Деплой](docs/deployment.md)** — инструкции по установке и настройке

## 🔌 Основные эндпоинты API

### Миграция из Keitaro
- `POST ?action=keitaro_import_sql` — импорт дампа Keitaro

### Кампании
- `GET ?action=campaigns` — список кампаний
- `GET ?action=get_campaign&id=X` — данные кампании
- `POST ?action=save_campaign` — сохранить кампанию
- `POST ?action=delete_campaign` — удалить кампанию
- `GET ?action=campaign_report` — отчёт по кампании

### Аналитика
- `GET ?action=metrics` — сводная статистика
- `GET ?action=chart` — данные для графика
- `GET ?action=trends` — детальная аналитика
- `GET ?action=conversions` — лог конверсий

### Интеграции
- `GET ?action=affiliate_networks` — партнёрские сети
- `GET ?action=traffic_sources` — источники трафика
- `GET ?action=telegram_settings` — настройки Telegram-бота

> 📖 **Полный список API**: см. [docs/api.md](docs/api.md)
> 🔑 **Авторизация по API-ключу** (для MCP/скриптов): заголовок `Authorization: Bearer <key>` или `X-Api-Key: <key>`.

## 📊 Модели выплат

| Модель | Описание |
|--------|----------|
| **CPC** | Оплата за клик |
| **CPuC** | Оплата за уникальный клик |
| **CPM** | Оплата за 1000 показов |
| **CPA** | Оплата за действие (лид) |
| **CPS** | Оплата за продажу |
| **RevShare** | Процент от дохода |

## 🔄 Импорт из Keitaro

### Подготовка дампа Keitaro

На сервере Keitaro выполните:

```bash
# Подключиться к серверу Keitaro
ssh root@YOUR_KEITARO_SERVER_IP

# Создать дамп
bash -lc '
source /etc/keitaro/env/inventory.env

cat > /root/keitaro-mariadb.cnf <<EOF
[client]
user=$MARIADB_KEITARO_USER
password=$MARIADB_KEITARO_PASSWORD
host=127.0.0.1
port=3306
protocol=tcp
EOF
chmod 600 /root/keitaro-mariadb.cnf

TABLES="keitaro_affiliate_networks keitaro_groups keitaro_offers keitaro_domains keitaro_campaigns keitaro_campaign_postbacks keitaro_landings keitaro_streams keitaro_stream_filters keitaro_stream_offer_associations keitaro_stream_landing_associations keitaro_traffic_sources keitaro_ref_sources"

mysqldump --defaults-extra-file=/root/keitaro-mariadb.cnf \
  --single-transaction --quick --skip-lock-tables \
  "$MARIADB_KEITARO_DATABASE" $TABLES \
  | gzip > /root/keitaro_orbitra_full.sql.gz

ls -lah /root/keitaro_orbitra_full.sql.gz
'

# Скачать файл
scp root@YOUR_KEITARO_SERVER_IP:/root/keitaro_orbitra_full.sql.gz .
```

### Импорт в Orbitra

1. Откройте **Миграции** в меню админки
2. Следуйте инструкции в блоке «Как создать бэкап Keitaro»
3. Загрузите файл `keitaro_orbitra_full.sql.gz`
4. Выберите, что импортировать (кампании, офферы, домены и т. д.)
5. Нажмите «Показать предпросмотр» для проверки
6. Нажмите «Импортировать в Orbitra» для реального импорта

## 🎨 Кастомизация

### Темы
Orbitra поставляется с несколькими встроенными темами — **светлая**, **тёмная**, **зелёная** и **неон** — плюс полностью **кастомная** тема, где вы задаёте свою палитру (основной цвет, фоны, текст). Выбор темы — в **Настройки → Брендинг**.

### Брендинг
Логотип, цвета и название настраиваются в **Настройки → Брендинг**.

### Язык интерфейса
Язык переключается в **Профиль → Настройки**. Доступно семь языков: английский, русский, украинский, испанский, китайский, французский и немецкий.

## 🛠 Технологии

| Категория | Технология |
|-----------|------------|
| **Бэкенд** | PHP 8.3+ |
| **База данных** | SQLite 3 |
| **Фронтенд** | React 19.2.0 |
| **Сборщик** | Vite 7.3.1 |
| **UI-фреймворк** | Tailwind CSS 4.2.0 |
| **Иконки** | Lucide React 0.575.0 |
| **HTTP-клиент** | Axios 1.13.5 |
| **Графики** | Chart.js 4.5.1 |
| **Даты** | date-fns 3.6.0 |
| **PHP-зависимости** | Composer |

## 📝 Что нового

### Текущий релиз — v1.2.0 (2026-08-23)

**Исправлено**
- 📱 **Панель на телефоне — виртуальный десктоп 980px** — не было meta viewport: ни один `lg:`-брейкпоинт не срабатывал; один тег + мобильная вёрстка делают панель пригодной для телефона
- 🧭 **Меню «Создать поток» мертво на таче** — hover → клик, цели 44px
- 🛟 **router.php съедал реальные файлы корня** — `/sw.js` умирал пустой 200 в ветке алиасов; guard `file_exists` повторяет `RewriteCond !-f`

**Добавлено**
- 📱 **Мобильная вёрстка** — пять списков ниже `lg` переходят на карточные строки (общий MobileCards); редактор кампании в одну колонку; тулбары переносятся
- 📲 **PWA + service worker** — манифест из admin.php (`start_url` совпадает с реальным входом, включая секретный путь); иконки + maskable + apple-touch; шелл network-first, хешированные ассеты stale-while-revalidate; обновление подменяется при перезагрузке через тост, не посреди сессии
- #️⃣ **Хешированные ассеты** — `index-[hash].js|css` через `.vite/manifest.json`; кэш-бастер `?v=` и совет про hard-reload ушли
- 🔄 **Автооптимизатор ротации** — Auto у списка лендингов/офферов + метрика (продажи/CR/EPV/EPC/ROI); крон */5, окно 7 дней, safe исключены, боты учтены; потолок 70% / пол 5% / ≤20пп за прогон; аудит по элементам (`stream_rotation_log`); сохранение кампании возвращает веса крону

Предыдущие релизы — v1.1.11:

**Исправлено**
- 🩹 **Цифры клоака расходились между экранами** — список кампаний с датой считал safe-клики, а Лендинги/Офферы/дашборд исключали; общий хелпер на всех четырёх поверхностях, `COALESCE(is_safe_page, 0)` сохраняет видимость до-обсервабельности (NULL) строк
- 🕐 **Окно панели клоака делится по дням в таймзоне отчёта** (было UTC) и возвращает точное рассчитанное окно
- 🛟 **Диагностика клоака переживает деградировавшую базу** (нет таблицы → откат, без фатала)

**Добавлено**
- 🔍 **Evidence в каждой причине клоака** — `crawler_or_tool_ua:curl/`, `iprange_datacenter:52.95.0.0/8`, `bot_isp:hetzner`, `geo_country:US`; видно в логе и деталях клика, срезается при агрегации/порогах
- 🔗 **Панель клоака → лог кликов** + вкладки ALL/SAFE/MONEY, фильтры часов/потока, ISP/ASN/тип прокси/вердикт в деталях
- 🛠️ **Self-heal DDL `cloak_suppressed_stats`** — таблица пересоздаётся на запуске для баз, помеченных ранней сборкой миграции (корень «пустой диагностики»)
- 📋 **Бот-ISP блэклист в формате Keitaro** — провайдеры на строку, целые фразы; родовые суффиксы (Inc/Ltd/GmbH) и <3 символов игнорируются с предупреждением в настройках (зеркало PHP/JS)
- ⭐ **Звезда Group By + последний применённый** в настройках отчёта; «Отчёт» в Кампаниях следует за единственной галочкой.

v1.1.10: 🩹 КРИТИЧНО прочерки «Лендингов», 🖱️ drag-and-drop колонок, 🎛️ слой навбара, 🔌 кнопки параметров из источника, ⚖️ «Разделить поровну» + живые доли; v1.1.9: 🩹 автолечение двойного `?`, 🔍 наблюдаемость клоака (W1–W4), 📊 паритет метрик Лендинги/Офферы; v1.1.8: 🎯 фильтры по сущностям в Аналитике (Тренд/Когорта) с фиксом ребилда диста; v1.1.7: 🛟 хотфиккс редирект-лупа ассетов в nginx (`nginx_sync.php` один раз); v1.1.6: 🔀 SSL mode selector, 🧰 smoke-тесты и диагностика, 🧩 гарантии загрузки ассетов, 🗑️ массовой импорт убран; v1.1.5: 🔐 SSL-менеджмент (ORB-014), 🧠 SUBID в логе трафика.

Полная история версий: [CHANGELOG.md](CHANGELOG.md).

## 🤝 Участие в разработке

Контрибьюции приветствуются! Пожалуйста:

1. Сделайте форк репозитория
2. Создайте ветку для вашей фичи (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Проект распространяется под лицензией MIT — см. файл [LICENSE](LICENSE).

## 📞 Поддержка

- **GitHub Issues**: https://github.com/fenjo26/Orbitra.link/issues
- **Документация**: [docs/](docs/)
- **Email**: info@orbitra.link

---

**Orbitra** — современный трекер для арбитражников и вебмастеров.

**Теги**: `tracker`, `affiliate-marketing`, `keitaro-alternative`, `php-tracker`, `react-admin`, `cpa-network`, `traffic-management`, `split-testing`, `conversion-tracking`
