# Основные Функции Orbitra v1.3.9

Полный обзор функциональности трекера Orbitra — современного аналога Keitaro Tracker.

## 🆕 Новое в v1.3.9

- **Трёхсостояний вердикт цепочки SSL** — `orbitraChainVerdict()`: ok / incomplete_chain / chain_unreadable; «файл нечитаем» = статус installed с предупреждением (оранжевая иконка в панели) и без расхода попыток; привилегированное чтение публичных файлов цепочки через sudoers (privkey никогда) и проверка существования линии через `sudo certbot certificates` без новых привилегий; перевыпуск — `sudo certbot delete` с проверкой exit-кода + `--force-renewal`; миграция 41 сбрасывает ложный бэкофф (schema 41).
- **Сертификат при парковке домена** — синхронный запуск воркера во всех четырёх точках сохранения домена (вместо выстрела в `/dev/null`), отчёт воркера в ответе API, `set_time_limit(120)`, синхронный прогон ≤3 доменов; cron каждые 5 минут с апгрейдом старой часовой линии на месте; flock против коллизий certbot-лока; записи воркера через `orbitraSslWriteWithRetry`; спиннер и честный текст на кнопке (×7 локалей).
- **LeadForge без фейковых успехов** — `default:` без адаптера: лог + снапшот в CRM + честный 502 вместо thank-you; SSOT `leadforge_networks` с `has_adapter`; адаптер AdCombo; m1/monsterleads честно недоступны (спеки кабинет-only); группа «Мои партнёрки» из affiliate_networks с префиллом endpoint и оффером встроенного адаптера по сигнатуре имени; buildBundle отклоняет unknown/no_adapter/custom-без-URL.
- **Защита от сканирования** — privacy_* в обоих белых списках с кросс-валидацией (redirect ⇒ валидный URL), строго unknown_settings, исполнение в index.php: 302/404/blank на неизвестные алиасы.
- **Домены** — гейт custom-SSL полей (ssl_source), таблица в общей fixed-системе (9 колонок, разделители, центрирование), двухрядный тулбар, реорганизованная модалка.
- **Установщик** — ufw 80/443 при активном файрволе, крон воркера */5, `cli/ssl_diagnose.sh` (read-only, авто-вердикт).

## 🆕 Новое в v1.3.8

- **Многоточие только на настоящем тексте** — `text-overflow` физически не умеет укорачивать атомарные инлайны (flex-обёртку, input, кнопку с иконкой), и на переполненной ячейке из контролов браузер дорисовывал «…» рядом с полностью видимым содержимым; теперь td/th трекер-таблиц обрезаются (clip) по умолчанию, а многоточие просят только текстовые ячейки (`.cell-text`: ID, группа, метрики, URL, тоталы) — там, где title-тултип показывает полное значение; plain-заголовки (Actions, Payout) — через `th.cell-text`.
- **Чекбокс-колонка без переполнения** — 40px минус два паддинга по 14px оставляли 12px под чекбокс шириной 14px: постоянное переполнение и постоянное «…» у каждого чекбокса; колонка получила нулевые боковые отступы (`.col-check`) и больше не переполняется ни в одной из таблиц.
- **Значения по центру под центрированными заголовками во всех трекер-таблицах** — включая числа (инлайновый `textAlign: right` убран из Campaigns/Offers/Landings/CampaignReports), кнопки действий центрируются следом (`.action-buttons` → `justify-content: center`); единственное исключение — дерево отчётов (`report-pinned-name`): там строки сдвигаются паддингами по глубине и центрирование схлопнуло бы иерархию.
- **Гигиена кода** — все 31 ошибка ESLint в четырёх компонентах трекер-таблиц погашены: опциональный catch-биндинг с коротким комментарием-причиной у осознанно игнорируемых ошибок, мёртвый стейт (`settingsOpen`), неиспользуемый импорт `nextSortState` и осиротевший хелпер `entityLabel` удалены, дубль ключа `epv` в объекте тоталов Campaigns снят; 5 предупреждений `exhaustive-deps` оставлены — смена зависимостей хуков меняет поведение и требует разбора по случаям.

## 🆕 Новое в v1.3.7

- **Ротация всех колонок таблицы кампаний** — единый персистентный порядок (`orbitra_campaign_col_order`) управляет шапкой, строками, тоталами и colgroup: фиксированные колонки (ID/Status/Campaign/Actions/Group) и метрики перетаскиваются одинаково, порядок переживает reload; рассинхрон «шапка сказала одно, строки нарисовали другое» невозможен архитектурно. Реконсилация держит порядок в согласии с кастомайзером и finance-фильтрами без миграций хранилища.
- **Стрелки сортировки + ресайз на фиксированных колонках** — ID/Status/Group снова показывают шевроны (сортировка работала всегда, аффорданс был скрыт с v1.3.2); все пять колонок получили хендлы ресайза с персистом ширин; Actions — через общий `SortableTh` с новым пропом `sortable={false}` (гриф + ресайз без стрелки: сортировать нечего).
- **Жёсткая обрезка ячеек** — `overflow: hidden` + `text-overflow: ellipsis` на всех th/td трекер-таблиц: суженная колонка раньше рисовала nowrap-контент прямо поверх соседней ячейки (обрезал только заголовок через `.resizable-th`); строки непрозрачны (hover по-прежнему меняет строку целиком), иконки/кнопки (`flex-shrink: 0`) сохраняют размер и режутся, а не деформируются.
- **Центрирование и выравнивание** — заголовки всех таблиц по центру колонки (при расширенной пользователем ширине подпись не жмётся к краю; данные сохраняют смысловое выравнивание — цифры справа, имена слева); vertical-middle для `.page-table` (Domains/Networks/Sources/Trends/Conversions).
- **Мелочи таблиц** — длинное имя кампании — многоточие внутри ячейки (truncate не работал на inline-span); Actions 150px под четыре контрола; кнопки Namecheap рендерятся сразу и disabled до ответа статуса; драг колонки показывает непрозрачный чип с её именем вместо полупрозрачного снимка заголовка, drop-цель — плотный фон + акцентная полоса.

## 🆕 Новое в v1.3.6

- **Колонка Visitors считает посетителей** — раньше это был алиас глобальной уникальной суммы (4 места в SQL), и панель показывала Clicks > Visitors; теперь `COUNT` по тем же отфильтрованным строкам, уникальность — в своей колонке. Строка тоталов Campaigns зеркалит формулы бэкенда (cpv/epv/eCPC/eCPM ÷clicks) и получила ключи visitors/uClicks.
- **Кеш статусов рекламных сущностей (schema 40)** — тумблеры отчёта (Ad/AdSet/Ad Campaign) отвечают из кеша: успех 5 минут, фейл 15; кешированный фейл не отдаёт фейковый ACTIVE, тумблер из панели инвалидирует свою запись. Раньше каждое открытие отчёта — ~25 живых Graph-запросов, при rate limit — вечно безуспешных.
- **Поллинг дашборда только на дашборде** — 7 эндпоинтов каждые 10с на каждом табе (~60 тыс. запросов/день в один SQLite) теперь: только таб Dashboard, 15с, пауза в скрытых табах.
- **Диапазон дат переживает перезагрузку и навигацию** — Campaigns (localStorage) и отчёт (sessionStorage, кнопка Report открывает свежий диапазон); пресет хранится id и пере-выводится через `getPresetDates`; пикер подсвечивает правильный чип (`initialPreset` + preset id третьим аргументом onChange).
- **Отчёт в URL** — оверлей зеркалится в `#report` / `#report/<id>` (replaceState): refresh возвращает в тот же отчёт.
- **Отложенный DNS-refresh устойчив** — statement готовится на каждой итерации + retry на «database is locked» (переиспользование handle между запросами резолвера падало с SQLITE error 21 на FPM).
- **Мобильный редактор** — ряды ротации переносятся (`basis-full` имени ниже `sm`), внутренняя группа тулбара ротации переносится на всех 5 сайтах, Conditions-поповер и панель датапикера — bottom-sheets, чипы пресетов не сжимаются (`flex-shrink-0`).
- **Мелочи панели** — Actions в Campaigns 150px под четыре контрола; кнопки Namecheap рендерятся сразу и disabled до ответа статуса.

## 🆕 Новое в v1.3.5

- **Мобильные карточки для Доменов / Партнёрских сетей / Источников трафика** — ниже `lg` три страницы рендерятся стек-карточками вместо таблиц с 7–9 колонками; сложные ячейки (статус домена с семью ветками, SSL, действия, URL источника) вынесены в общие render-хелперы — десктоп-таблица и карточки зовут один код и разъехаться не могут.
- **Модалки на телефоне = полноэкранные шиты** — ниже 480px `100dvh` + safe-area вместо центрированной карточки, прятавшей футер длинных форм под нижний край; геометрия шита `!important` против инлайновых стилей модалок (только внутри media-блока).
- **Тайтлы мобильных карточек — двухстрочный clamp** — длинные имена кампаний/офферов больше не обрезаются до общего префикса; действия переехали в строку субтайтла, форма шапки единая.
- **Тулбары отпускают фиксированные ширины на <480px** — контролы делят строки, поиск занимает полную строку, избыточные лейблы («Columns», «Search:») скрываются.
- **Отчёты и таблицы на телефоне** — закреплённая колонка имени открепляется <480px (явный класс, тотал не задет); сетка фильтров конверсий не переполняется; пять overflow-ловушек `min-width:auto` исправлены; `overflow-x: clip` на `<main>` (не `hidden` — тот сломал бы sticky-заголовки); `touch-action: pan-x` на отчётной таблице против осевой блокировки iOS.
- **DNS-кэш доменов честно устаревает** — TTL 30 минут реально работает: протухший статус отдаётся мгновенно и обновляется после ответа через `fastcgi_finish_request` (inline-фоллбэк без FPM); debug-лог на каждый лукап убран из `php_errors.log`.
- **SSL-очередь** — `ssl_source` добавлен в SELECT очереди; при override явного выбора (Let's Encrypt / свой) в `ssl_error` пишется переводимый код `awaiting_dns_for_ssl_switch` вместо молчаливой очистки (7 локалей).
- **Логин** — ошибка входа отдаёт код `invalid_credentials`, фронт мапит его через `t()`; английская панель больше не показывает русскую прозу при неверном пароле.

## 🆕 Новое в v1.3.4

- **Переключение Cloudflare-прокси работает по-настоящему** — тумблер в карточке домена меняет `proxied`-флаг самой DNS-записи в Cloudflare (`save_domain` → `orbitraCloudflareSyncDomain` → `upsertDnsRecord`), чистит кеш DNS-статуса и возвращает результат как `cloudflare_sync`. Переключение на Let's Encrypt одним сохранением: прокси снимается, домен встаёт в очередь certbot.
- **`ssl_source` — единственный источник истины режима SSL** — тумблер и кнопки (Let's Encrypt / Cloudflare / Свой) согласованы в обе стороны; модалка редактирования загружает и сохраняет `ssl_source` + пути кастомного сертификата (раньше открытие-сохранение их стирало).
- **Импорт доменов из Namecheap** — починена кнопка на странице Domains (React-событие уходило в `account_id`, запрос не покидал браузер).
- **Диагностика ошибок панели** — 38 catch-сайтов показывают реальное сообщение вместо константы «сетевая ошибка».
- **Устойчивость SSL-очереди** — блокировка SQLite при записи флага Cloudflare больше не прерывает часовой проход крона.
- **IPv4 во внешних API** — Namecheap, Cloudflare, CloudDetector и курсы валют пиннят `CURLOPT_IPRESOLVE_V4` как рекламные API.
- **Колонка SSL-статуса** — источник сертификата (Let's Encrypt / Cloudflare / свой / самоподписанный) подписан рядом с иконкой.
- **i18n** — устранена коллизия дублирующегося ключа `domains.sslCloudflare` во всех 7 локалях (тайтл статуса перезаписывал лейбл кнопки).

## 🆕 Новое в v1.3.3

- **Минификация продакшн-сборки** — `esbuild`: JS-бандл 5,26 МБ → 3,16 МБ (891 кБ gzip), поведение не меняется; повторные сборки побайтово идентичны (tailwind больше не сканирует собственный `dist/`).
- **Новые размерности отчётов** — Group By пополняется `isp`, `asn`, `param_campaign_name`, `param_adset_name`, `param_ad_name`, `param_utm_placement`, `param_source`; пикер и карты подписей/i18n выводятся из единого реестра `REPORT_DIMENSIONS` (раньше — четыре согласующихся списка, три падали молча); ключи добавлены в 7 локалей.
- **Экран входа на темах** — хардкод gray/indigo/white заменён токенами, следование всем 12 режимам.
- **Pre-paint темы** — `index.html` применяет сохранённую тему до первой отрисовки + сплэш в `#root` до монтирования React: тёмные темы не вспыхивают светлым.
- **Баннеры обновлений на warning-токенах** — дискиссис привязан к версии и переживает перезагрузку; клавиатурный путь у кликабельной зоны.
- **Чистка выделения кампаний при закрытии отчёта** — «Удалить выбранные» не целится в скрытые строки.

- **Чтение реального состояния рекламных сущностей** — `FacebookAdsEngine::fetchEntityStatuses()`: по одному запросу `/{id}?fields=id,status,effective_status` на сущность (Graph v26 удалил батч `?ids=` и применяет v26-поведение независимо от версии); сбой оставляет `error_log` с id и HTTP-кодом. Новый экшен `ad_entity_statuses` в api.php: кампании трекера — из БД, сущности сетей — через Graph; общий резолвер подключения `orbitraResolveFacebookConnectionId()` (читает и пишет одним и тем же токеном); частичный ответ = успех.
- **Тумблеры отчёта согласованы с сервером** — мердж в CampaignReports отдаёт приоритет свежим серверным данным, оптимистичная отметка защищена только на время полёта команды (`togglingIds` в зависимостях эффекта); пауза переживает переоткрытие отчёта. Бейджи `Disapproved` / `In review` (7 локалей) показывают состояния, которых нет в дереве отчёта.
- **IPv4 на всех Graph-запросах агрегатора** — `CURLOPT_IPRESOLVE_V4` в `httpGet()` и `updateEntityStatus()` (непригодные AAAA graph.facebook.com).
- **Guard сохранённых колонок Лендингов** — `FIXED_LANDING_COLUMN_IDS` + обратная запись очищенного списка, порт защиты Офферов; id полей сущности больше не рендерятся вторыми колонками с сырыми именами.
- **Таблица Кампаний** — columnDefs зеркалит порядок рендера (рассинхрон colgroup рисовал колонки чужой шириной); Actions на пятой позиции; шесть фиксированных колонок без ресайза с одноразовой чисткой устаревших ширин; шевроны сортировки скрыты на ID/Статусе/Группе (`hideSortIcon` в общем SortableTh); алиас-чипы убраны из строки и мобильной карточки; меню строки тримнуто до Расхода/Дубликата/Сброса статистики/Удаления.
- **Копирование ссылки кампании** — тихий копи + тост; тематическая `CampaignUrlModal` (URL одной выделенной строкой, макросы подсвечены) только при отказе обоих транспортов клипборда; `window.alert` с сырой ссылкой убран.
- **Клипборд на plain-HTTP** — Настройки постбеков, Feedback и MCP копируют через `utils/clipboard` (execCommand-фолбэк), успех подтверждается только реальным результатом.
- **Датапикер в Логе конверсий** — модалка больше не скроллится сама (`overflow: visible`), вертикальный скролл живёт на обёртке таблицы; Apply/Cancel и выбор таймзоны достижимы; кнопка-триггер не переносится внутри даты (`whitespace-nowrap`).
- **Эксплуатация** — `install.sh` включает php-fpm `catch_workers_output` (приложный `error_log()` больше не выбрасывается) и ставит крон оптимизатора ротации каждую минуту (интервал перерасчёта 5 минут = 5 минут).
- **UI оптимизатора** — `floor_pct` убран из условий (дефолт из `ROTATION_DEFAULTS` по-прежнему применяется, `floor_pct >= cap_pct` в кроне сохранён).

## 🆕 Новое в v1.3.1

- **Расход сводится в таймзоне рекламного кабинета** — `core/CostImporter.php` определяет таймзону кабинета (оверрайд опцией → поле в field mapping → креды подключения → кэшированный на сутки live-запрос `FacebookAdsEngine::accountTimezone()`) и сдвигает `created_at` кликов при сопоставлении с датами расхода. Смещение вычисляется на самом дне расхода, поэтому границы DST ложатся корректно. Не-UTC кабинеты (Asia/Kolkata и т.п.) впервые сводятся без unmatched-дней.
- **Safe-клики исключены из атрибуции расхода** — расход больше не размазывается по заклоакированному краулерному трафику, который отчёты исключают (`COALESCE(is_safe_page, 0) = 0`, NULL считается money-стороной); для периодов вообще без money-кликов — фолбэк на все клики со счётчиком `safe_page_fallbacks` в статистике импорта.
- **`campaign_report` уважает исключение safe-страниц** — фильтр v1.1.11 пропустил отчёт; на живом cloak-стриме отчёт завышал клики в 4 раза со всеми производными метриками.
- **CAPI без тихих потерь** — `enqueue()` Facebook/TikTok при статусе без маппинга пишет строку в `system_logs` (INFO для сознательно пустых маппингов, WARNING для остальных): кастомный тип конверсии, перекрывающий встроенный статус (`hold` в COD), больше не съедает лиды молча. Кнопка теста в Pixel Vault прогоняет каждый реальный транспорт профиля (по `campaign_pixels`, включая прокси, креды вырезаются из ответа). curl везде пинится на IPv4 с коннект-бюджетом 10с — непригодные AAAA graph.facebook.com перестают выглядеть сбоем резолвера.
- **`install.sh` ставит кроны очереди постбеков и агрегатора расхода** — воркер `postback_queue_cron.php` каждую минуту (берёт лок, наложения бесплатны), `aggregator_cron.php` каждые 15 минут; тот же маркер-паттерн, что у остальных кронов.
- **Здоровье фоновых воркеров** — экшен `worker_health` в api.php + янтарный баннер в панели (7 локалей): воркер очереди не запущен/застрял (возраст pending-событий), агрегатор не по расписанию (heartbeat `aggregator_last_run_at` от крона), кастомные типы конверсий перекрывают встроенный статус без события Meta.
- **Таймзона работает по-настоящему** — Кампании и Лог конверсий шлют `?timezone=`; общий стор `utils/useTimezone.js` (module-value + подписчики + межвкладочный storage-sync) заменяет шесть приватных `useState`; дашборд рефетчится при смене зоны; смещение SQLite для отчётов привязано к середине запрошенного диапазона, а не к «сейчас» — корректный DST для диапазона внутри одного периода.
- **Панель** — модалки над навбаром по задокументированной шкале z-index (2000/2050/2100); ширины колонок переживают гонку lostpointercapture/pointerup в Chrome; заголовки Лендингов сортируемые (общий `SortableTh.jsx` вместо дублей в Кампаниях/Офферах); id полей сущностей не утекают в метрики Офферов (санитизация при загрузке и сохранении); футеры тоталов выровнены с ячейками (14px — контракт); нулевые конверсии не зелёные; SQLite-флаги не рендерятся нулями; подсказка имени кампании идемпотентна; чип таймзоны без подчёркиваний; сентинел-строки IP2Location не попадают в поля гео.

## 🆕 Новое в v1.3.0

- **Изменяемая ширина колонок во всех таблицах** — общий модуль `ColumnResize.jsx` даёт таблицам Кампаний, Офферов, Лендингов, Логов, Конверсий, отчёта кампании и настроек отчёта drag-to-resize заголовки: тяните край заголовка, ширина меняется живьём в процессе, персистится на каждую таблицу (localStorage) и сбрасывается к дефолту. Нагруженная деталь реализации: `overflow:hidden` на ячейке заголовка с хэндлом — без него sticky-заголовок следующей колонки закрашивает соседний хэндл.
- **Модалки лога кликов и лога конверсий** — `ClickLogModal.jsx` переносит лог кликов в модалку с быстрыми фильтрами клоака (ALL/SAFE/MONEY), W1/W2-полями в деталях клика и фильтрами часов/потока; открывается и из списка Кампаний, и из карточек потоков в редакторе кампании. Парная `ConversionsLogModal.jsx` — для конверсий.
- **Общий сборщик URL кампании** — `utils/campaignUrl.js`: список Кампаний и редактор кампании строят ссылку одним хелпером вместо дублирования логики.
- **Колонка «Домен» в таблице Кампаний** — `api.php` джойнит `domains` по `campaigns.domain_id` и отдаёт `domain_name`.
- **kclient.php 2.0** — на вторичных страницах сайта `restoreFromQuery()` подхватывает клик из `_subid` и восстанавливает оффер *этого* клика из сессии (сверка по id — устаревшая сессия не отдаст чужой оффер; именно тут раньше `getOffer()` возвращал пустоту); `getOffer(42)` работает в форме голого id (int из PHP-кода, числовая строка из шаблона); выбор конкретного оффера *заменяет* `offer_id` трекера на transition-ссылке — парсер, читающий первое вхождение, больше не молча увозит на оффер трекера. Сниппет панели интеграций — один готовый к вставке блок с альтернативами комментариями рядом (как у Keitaro), плюс сниппет вторичной страницы и хелпер ссылки оффера; новая страница документации `docs/tracking-client-php.md` связана с панелью.
- **Макросы прямого назначения на пути KClient** — `core/click_api.php` подставляет `{clickid}`, `{subid}`, `{ip}`, `{country}` в direct-destination URL так же, как путь ссылки кампании: оффер-URL с `{subid}` (написание Keitaro, которое просят сети) больше не приезжает в сеть пустым, и постбек конверсии находит свой клик.
- **EPC без cost-гейта в автооптимизаторе ротации** — `orbitraRotationMetricNeedsCost()` гейтит только `roi_confirmed`: EPC = выручка ÷ клики, spend-слагаемого в ней нет, конфиги EPC работают на кампаниях без расхода. Закреплено в `tests/rotation_allocator_test.php` (zero-cost кампания держит EPC-конфиг) и `tests/rotation_cron_test.php` (EPC-прогон считает и пишет аудит с правильной метрикой 1.5).
- **Тест-сюит полностью зелёный** — четыре устаревших теста приведены к реальному поведению: `facebook_integration_test` использует пиксель-первую сигнатуру TikTok `resolveEvent` (сменилась в 7483d75), `postback_status_codes_test` ожидает запись неизвестного статуса для ретроактивного маппинга (ORB-012) вместо 400, DNS/nginx-регрессионные тести скипаются на хостах без фикстур/nginx вместо красного.

## 🆕 Новое в v1.2.0

- **Мобильная вёрстка панели** — корень «панель не для телефона» оказался тривиальным: во фронтенде не было meta viewport, мобильный браузер рендерил виртуальный десктоп 980px, и все `lg:`-брейкпоинты, уже написанные в коде, молча не срабатывали. Теперь `width=device-width, viewport-fit=cover`, и ниже `lg` пять списков (Кампании, Офферы, Лендинги, Конверсии, Логи) переключаются с таблиц на карточные строки через общий компонент `MobileCards.jsx`; редактор кампании раскладывается в одну колонку; пагинация и тулбары отчётов переносятся по строкам; меню «Создать поток» открывается по клику вместо hover (на тачскринах было мертво) с тач-целями 44px.
- **PWA** — манифест отдаёт сам `admin.php` (`?orbitra_manifest=1`), поэтому `start_url` и scope всегда совпадают с реальным входом панели: `/admin.php`, секретный админ-путь или корень домена на дев-роутинге — статический файл мог захардкодить только один из них. Иконки 192/512 + maskable-варианты + apple-touch-icon, theme-color, standalone-режим: панель устанавливается на домашний экран телефона как приложение.
- **Service worker (`/sw.js`)** — стратегии по типам ресурсов: документ панели (admin.php/секретный путь) network-first с офлайн-фолбэком (шелл отдаётся no-store, обычный reload всегда подхватывает новую сборку); хешированные ассеты `/frontend/dist/*` — stale-while-revalidate (имена содержат хеш, каждый файл неизменяем: новый релиз = новые URL, старые продолжают работать для сессий на старом шелле); api.php, click.php, лендинги и постбэки не перехватываются вовсе. `skipWaiting` сознательно нет: новая версия подменяется только по перезагрузке через тост «доступно обновление», никогда посреди сессии. Путь панели переносится на URL регистрации как `?panel=…` и переживает рестарты воркера.
- **Хешированные имена сборки** — vite переведён на `index-[hash].js|css` + `.vite/manifest.json`; `admin.php` резолвит входные ассеты по манифесту (regex-перепись тегов в собранном index.html заодно лечит устаревший шелл после частичного деплоя: ассеты обновились, index.html — нет). Кэш-бастер `?v=filemtime` и совет «после обновления сделайте hard-reload» уходят в историю: оболочка no-store, имена уникальны per-контент.
- **router.php больше не съедает реальные файлы корня** — запрос одноименного файла в корне домена (`/sw.js`) попадал в ветку алиасов кампаний (она стоит раньше статики) и умирал пустым 200 — именно это убивало service worker; guard `file_exists` перед веткой повторяет семантику Apache `RewriteCond !-f`.
- **Автооптимизатор ротации лендингов/офферов** — у каждого списка лендингов или офферов в потоке переключатель *Auto* и метрика (подтверждённые продажи, CR, EPV, EPC, ROI; cost-зависимые доступны только при наличии расхода у кампании). Крон (`rotation_optimiser_cron.php`, рекомендуется `*/5` — не-дью стримы пропускаются дёшево) пересчитывает веса тем же движком `core/ReportMetrics.php`, что и отчёты, по скользящему окну (по умолчанию 7 дней); клики safe-page исключаются, боты остаются. Ходы лимитированы: потолок 70%, пол 5%, не более 20пп суммарно за прогон (сходимость за несколько прогонов вместо прыжка), непрогретые элементы ниже min_sample держат равную долю, округление всегда в сумме 100. Каждое изменение веса пишется в `stream_rotation_log` (миграция 39): старый/новый вес, метрика, значение, размер выборки, окно, имена потока и элемента — строки keyed по rotation key из `schema_custom_json` (id стримов протухают при пересохранении кампании; копия кампании получает свежие ключи). Пока Auto включён, веса принадлежат крону: ручной ввод заблокирован, а `save_campaign` санитизирует auto-конфиги (cost-метрики требуют расход, клампы параметров) и возвращает крону пересчитанные веса — редактор раунд-трипит устаревшую копию и не должен воскрешать сдвинутые. Endpoint `rotation_status` отдаёт редактору доступность расхода и последние решения для карточек стримов; install.sh ставит крон в crontab www-data.

## 🆕 Новое в v1.1.11

- **Реконсиляция цифр клоака между экранами** — список кампаний с датой-фильтром показывал M+N (safe-клики попадали в клики/расход/CR), тогда как «Лендинги», «Офферы» и дашборд считали только M; теперь все четыре поверхности спрашивают один хелпер `orbitraSafePageExclusionNeeded()` (активная кампания + cloak-поток с непустым schema_custom_json), а сам фильтр везде `COALESCE(is_safe_page, 0) = 0` — строки с NULL (клики до миграции наблюдаемости и любые писатели, не заполняющие колонку) считаются money-стороной и не исчезают из отчётов молча (в SQLite «is_safe_page = 0» исключает NULL).
- **Evidence в кодах причин клоака** — каждая причина теперь строка `код:факт`: `crawler_or_tool_ua:curl/` (сигнатура из чёрного списка), `iprange_datacenter:52.95.0.0/8` (совпавший CIDR — `IpRanges::match()` возвращает саму строку списка), `bot_isp:hetzner` (совпавшая запись), `bot_blocklist:<IP или сигнатура>`, `geo_country:US`, `device_type:Mobile`, `ip2proxy_high_fraud:87`, `hosting_isp:aws`. Разделитель — первое «:» (IPv6-CIDR в evidence безопасен), запятые/переводы строк вычищаются, длина evidence ограничена 64 символами; старые строки без «:» валидны для всех читателей. Пороговая логика детектора и агрегация панели срезают evidence хелпером `reasonCode()` — группировка остаётся по слою детекции, вердикты не меняются.
- **Панель клоака → лог кликов** — карточка диагностики в редакторе кампаний открывает модалку лога преднастроенной ссылкой (route=safe/money, окно 24ч, поток); лог кликов получает вкладки ALL/SAFE/MONEY, фильтры часов и потока, а в деталях клика — сетевые факты решения (ISP, ASN, proxy_type, вердикт, причины с evidence).
- **Self-heal DDL для `cloak_suppressed_stats`** — базы, помеченные актуальной версией схемы ранней сборкой cloak-миграции, никогда её не перезапускали и оставались без таблицы suppressed-статистики (корень саппорт-кейсов «панель клоака пустая»); таблица пересоздаётся идемпотентно на каждом запуске (DDL байт-в-байт совпадает с миграцией), и даже деградировавшая/читанная база не роняет панель — suppressed откатывается к 0.
- **Окно `cloak_summary` в таймзоне отчёта** — панель делила по дням в UTC, тогда как соседний список кампаний — в таймзоне отчёта (users.timezone, `?timezone=`); теперь то же смещение `$dbTzOffset`, что у всех остальных поверхностей, плюс ответ раскрывает точное окно (`window: from/to/timezone`), чтобы две цифры на соседних экранах не могли молча означать разные периоды. День таблицы suppressed хранится в UTC и на границе таймзон может отставать на сутки — счётчик вспомогательный и подписан окном.
- **Бот-ISP блэклист в формате Keitaro** — список «Провайдеры» (cpa.rip keitaro-isp-base): один провайдер на строку, сопоставление целой фразой — пунктуация принадлежит имени («Amazon.com, Inc.», «ZSCALER, INC.», апострофы, скобки, слэши); строка делится по запятым только когда КАЖДЫЙ сегмент — правдоподобная самостоятельная запись (легаси-формат через запятую), пробелы не делят никогда. Родовые корпоративные суффиксы (inc/ltd/llc/limited/gmbh/corp/co/sa/ag/bv/oy/pty/plc/network/services) и записи короче 3 символов игнорируются — иначе «Inc» матчил почти любого провайдера и уводил реальный трафик на safe. Матчинг — lookarounds вместо `\b` (запись может кончаться точкой), точка на конце записи опциональна. Фронтенд-зеркало `frontend/src/utils/botIspList.js` предупреждает об игнорируемых записях прямо у textarea настроек — пара PHP/JS синхронизирована дословно.
- **Звезда Group By и последний применённый** — в настройках отчёта группировку можно закрепить звёздочкой: она станет дефолтом для новых отчётов; при отсутствии звезды берётся последний применённый вариант (orbitra_report_group_by), затем country; сброс настроек снимает оба ключа, удаление звезданного шаблона — звезду.
- **Кнопка «Отчёт» следует за выделением** — в списке кампаний ровно одна отмеченная галочка открывает отчёт этой кампании (подсказка на кнопке показывает имя), ноль или несколько — общий отчёт.

## 🆕 Новое в v1.1.10

- **Критический хотфикс «Лендингов»** — рерайт таблицы из v1.1.9 передавал объект колонки (`{id, label, fixed}`) в рендерер ячейки, который сравнивает строковые id: ни один case не совпадал, все семь фиксированных колонок (ID, Статус, Имя, Группа, Тип, URL, Last Event) падали в `default: '-'`, а чекбокс из тела строки был отфильтрован — метрики съезжали на колонку влево относительно шапки. Теперь тело рендерит все фиксированные ячейки (включая чекбокс), счётчики колонок шапки и тела совпадают.
- **Drag-and-drop колонок таблиц** — в Кампаниях и Офферах перенос колонки за grip не стартовал по трём причинам сразу: grip находился внутри `<button>` сортировки (нативный drag не начинается на интерактивном потомке), компонент шапки определялся в теле компонента и перемонтировался на каждом рендере (подсветка dragover убивала перенос), а payload `dataTransfer` не выставлялся (Firefox без `setData` вообще не начинает drag). Grip стал отдельным источником переноса, шапка вынесена на module scope, payload выставляется; в отчётах кампаний — payload-фикс. Перенос сохраняется в localStorage.
- **Слой навбара** — выпадашки шестерёнки и меню пользователя рисуются поверх оверлея отчёта и настроек дашборда (уровень навбара поднят над страничными оверлеями); истинные модалки и мобильный drawer сохранили свой порядок слоёв.
- **Кнопки трекинг-параметров ведутся из источника трафика** — «Параметры Facebook» и «Добавить все трекинг-параметры» строятся из `traffic_sources.parameters_json` кампании (тот же контракт `{alias, param, macro}`, по которому пишет `orbitraSourceParamAliases()`): первая эммитит `param={{macro}}` по строкам источника в его порядке, вторая — `param={alias}` плюс нативные макросы трекера (subid, clickid, country, ip, cost, sub_id_1..3). Пресет Direct-URL больше не стирает ручные query-параметры: существующая строка парсится, сгенерированные ключи мержатся, значения пользователя выигрывают на коллизиях, дубликатов ключей нет, повторный клик — no-op. До выбора источника — универсальные Facebook-дефолты с подсказкой (переведено на 7 языков).
- **«Разделить поровну» и живые доли в ротации потока** — в заголовках списков офферов и лендингов каждого потока появилась кнопка выравнивания весов: деление только по включенным элементам (фильтр дословно повторяет `selectWeightedItem()` — state disabled/paused, is_active false/0/'0'), остаток округления достаётся первому включенному, выключенные сохраняют свой вес. Статичный «%» у поля веса заменён бейджем живой доли — вес / сумма включенных, один знак после запятой, «—» пока элемент на паузе; затемнение строк использует тот же фильтр, что и роутер.

## 🆕 Новое в v1.1.9

- **Автолечение двойного `?` в query-строке** — если в поле «URL-параметры» Facebook Ads стоит ведущий `?` или сторонняя клоака приклеила параметры к ссылке, где `?` уже был, PHP проглатывал все ключи между двумя `?` в значение первого параметра — включая routing-ключи (`token` / `campaign`). Теперь кривая строка чинится до резолва кампании во всех трёх точках входа (index.php, click.php, Click API v3): повреждённое значение первого ключа восстанавливается, «проглоченные» параметры возвращаются; ключ `utm_placement` добавлен в whitelist захвата. Лечение включается только при литеральном `?` в query-строке — обычный трафик не затрагивается.
- **Кнопка «Параметры Facebook» в редакторе кампаний** — копирует готовую строку трекинг-параметров (без ведущего `?`) со всеми макросами Meta (campaign.id, adset.id, ad.id, placement и т.д.) плюс кастомными параметрами кампании — для вставки в Meta Ads Manager.
- **Пресет «Добавить все трекинг-параметры» для Direct-URL** — одним кликом дописывает в URL потока полный набор макросов ({subid}, {clickid}, {campaign_id}, {adset_id}, {ad_id}, utm_*); неразрешённые `{макросы}` вычищаются из финального редирект-URL, чтобы литералы не уезжали в партнёрскую сеть.
- **Наблюдаемость клоака (полная реализация спецификации)** — каждое решение о маршрутизации теперь сохраняется и видно в панели:
  - клики несут колонки `cloak_verdict` (money / passive_safe / targeting_safe / js_safe), `cloak_reasons` (коды причин: geo_country, crawler_or_tool_ua, ip2proxy_vpn_proxy, …), `is_safe_page`, а также сетевые факты решения — ISP, ASN, proxy_type и уровень sensitivity; общий логгер `core/click_logger.php` пишет их одинаково во всех трёх точках входа;
  - в Analytics → Clicks добавлены колонки «Маршрут / Причина / Назначение / ISP / ASN» и фильтры по кампании, маршруту и причине; карточка клоака в редакторе кампаний показывает живую диагностику (`cloak_summary`);
  - клики Safe Page теперь **записываются по умолчанию** (`log_safe_clicks`, с `is_safe_page=1`) — «ноль кликов» больше не может означать «всё подавлено»; флаг «Исключить клики Safe Page из отчётов» (`exclude_safe_from_reports`) держит метрики чистыми, не пряча клики из лога; каждый подавленный хит попадает в счётчик `cloak_suppressed_stats` по кампании/потоку/дню/вердикту; старые потоки сохраняют прежнее поведение через миграцию схемы v38;
  - гео-безопасность: при включённом фильтре по странам без гео-базы посетители «Unknown» помечаются причиной `geo_unknown` (не `geo_country`) и маршрутизируются по настраиваемому `geo_unknown_action` (safe по умолчанию / money); редактор кампаний и страница «Гео-базы» предупреждают, когда фильтры работают без читаемой базы (`geo_targeting_ready` в `global_settings`), включая деградацию ISP-блоклиста без ASN-базы;
  - операторское руководство переписано в `docs/cloak-how-it-works.md`.
- **Полный паритет 65+ метрик на страницах «Лендинги» и «Офферы»** — регистрации, депозиты, боты/прокси, выручка по каждому статусу (hold/rejected/trash/registration/deposit), real revenue/profit/ROI, уники по потоку/глобально и среднее время LP→offer теперь реально считаются: раньше SQL не выбирал эти счётчики, колонки показывали нули, а real_roi — фиктивные −100%. Обе страницы получили настраиваемую таблицу метрик (пресеты, кастомизация колонок, строка итогов) как у кампаний.
- **Ротация потоков уважает выключатели элементов** — индивидуально выключенный (paused/disabled) оффер или лендинг внутри кастомной схемы потока больше не получает трафик; взвешенный выбор фильтрует неактивные элементы до подсчёта весов (исправлено во всех трёх точках входа).
- **Локали в полном паритете (7 языков)** — ключи «Параметры Facebook», пресета трекинг-параметров и «Группа / Продукт» добавлены во все локали (раньше приехали только в английскую); плавающий виджет браузерного расширения запоминает позицию перетаскивания.

## 🆕 Новое в v1.1.8

- **Фильтры по сущностям в «Аналитике»** — мультивыбор кампаний, офферов и лендингов в режимах Тренд и Когорта: дропдауны с группами (галочка на группе выбирает все её элементы), поиск внутри списка, «Выбрать все» / «Сбросить», бейджи активных фильтров с быстрым сбросом. Выбор уходит в параметризованные `IN (...)`-запросы через whitelist-поля `campaign_id` / `offer_id` / `landing_id` (последнее добавлено в whitelist'ы трендов и когорт в этом релизе).
- **Исправлено: фильтры из прошлой попытки не появлялись** — коммиты фичи не пересобирали `frontend/dist`, и панель продолжала отдавать старый бандл; Когорта к тому же молча игнорировала выбранные фильтры (эффект загрузки не перезапускался при выборе). Дропдауны сущностей переведены на лёгкие эндпоинты `campaigns_simple` / `offers_simple` (оба теперь отдают `group_id`), пустое состояние «Ничего не найдено» переведено на все 7 языков. **После обновления один раз сделайте hard-reload панели (Ctrl/Cmd+Shift+R)** — имя файла `index.js` стабильное, браузеры кэшируют старую сборку.

## 🆕 Новое в v1.1.7

- **Хотфикс ассетов лендингов за nginx** — генерируемая локация `/_internal_assets/` содержала вложенный regex-блок под `alias`; вложенные regex-локации ломают наследование alias в nginx, и передача ассетов уходила в редирект-лупы (500). Блок выпрямлен: без вложенных локаций — белый список расширений и MIME-проверки остаются в PHP, где они и были. PHP-фолбэк дополнительно распознаёт сломанный вариант конфига по шаблону (`broken_nested_regex_detected`) и отдаёт ассеты напрямую, пока на сервере не выполнен `nginx_sync.php`. Также починён порядок в генераторе vhost: список self-signed доменов теперь вычисляется до использования в Cloudflare-блоке. **На работающем nginx-сервере нужно один раз выполнить `sudo php /var/www/orbitra/cli/nginx_sync.php`.**

## 🆕 Новое в v1.1.6

- **Выбор режима SSL в «Доменах»** — три режима на домен: Let's Encrypt (автовыпуск), Cloudflare (прокси; генерируемый vhost восстанавливает реальный IP посетителя из диапазонов Cloudflare IPv4+IPv6 и определяет HTTPS по `CF-Visitor`), свой сертификат (пути к cert+ключу). Строки UI на всех 7 языках.
- **Smoke-тесты установки и диагностика** — `install.sh` после установки проверяет наличие nginx-локации `_internal_assets`, записываемость ACME-webroot, наличие self-signed сертификата и синтаксис конфига nginx; `cli/check_landings.php` диагностирует права на директории landings/ и offers/, полноту конфига nginx, доступность ACME-webroot, наличие SSL-сертификатов и отдачу тестового ассета.
- **Гарантированная загрузка ассетов лендингов/офферов** — HTML перед вставкой base-тега переписывается: абсолютные пути ассетов (`/css/style.css`) становятся относительными (`./css/style.css`), JS-полифилл сохраняет плавную прокрутку по якорям под base-тегом. Referer-фолбэк распознаёт URL кампаний (`/bd86o7dw`), разрешая лендинг/оффер по настройкам потоков. При отсутствии nginx-локации `/_internal_assets/` включается фолбэк: ассет отдаётся напрямую из PHP с поддержкой Range-запросов (206 Partial Content).
- **Удалено** — массовой импорт .txt/.csv (кнопки в Кампаниях, Офферах, Лендингах и Источниках) убран намеренно.

## 🆕 Новое в v1.1.5

- **Управление и проверка SSL (ORB-014)** — `core/ssl_manager.php` проверяет покрытие сертификатом для каждого домена; сгенерированный vhost nginx получает HTTPS-блок с self-signed сертификатом для доменов без Let's Encrypt и Cloudflare; настройка Cloudflare Full (Strict) описана в `docs/CLOUDFLARE_FULL_STRICT_SSL.md`; форма vhost зафиксирована регрессионным тестом на 279 проверок (`tests/nginx_config_regression_test.php`). **На работающем nginx-сервере нужно один раз выполнить `sudo php /var/www/orbitra/cli/nginx_sync.php`**.
- **Свои SSL-сертификаты для доменов** — загрузка сертификата и ключа в настройках домена (UI «Домены», все 7 локалей).
- **SUBID в логе трафика** — значения SUBID читаются из параметров клика на пути клика и в LeadForge; тесты покрывают отсутствующий ключ, битый JSON и NULL.
- **Массовая загрузка файлов** — общий компонент `BulkFileUpload` с разбором CSV и ошибками по каждому файлу, подключён в Bot Settings, «Персонализации», Кампаниях, Лендингах, Офферах и Источниках; инспектор `cli/check_landings.php`.
- **Прочее** — переработан контроль доступа по IP (`core/ip_access.php`); фронтенд-бандл пересобран — UI SSL и массовой загрузки, закоммиченные без пересборки, наконец едет в дисте.

## 🆕 Новое в v1.1.4

- **Сниппеты трекинга переведены на английский** — код-блоки во вкладке «Метод подключения» редактора кампаний (KClient JS/PHP, скрипт отслеживания, баннерные блоки, пиксели клика и конверсии, countdown / back-button / exit-intent, WordPress-шорткоды) несли хардкодные русские комментарии прямо в шаблонах — независимо от языка панели. Теперь все комментарии шаблонов на английском, переведены по месту (намеренно без локале-ключей: это код, который пользователь вставляет на свой сайт). Единственная оставшаяся русская строка — намеренная: `text_ru="Купить"` в примере WordPress-шорткода демонстрирует сам параметр мультиязычности.

## 🆕 Новое в v1.1.3

- **Восемь палитрных тем** — четыре цветовые схемы, каждая в светлой и тёмной вариации, выбираются в «Персонализации»: Кобальт (сигнальный синий на белом и чистом чёрном), Канарейка (чёрные кнопки на белом; канареечный жёлтый на графите), Пергамент (тёплый кремовый фон с чернильными кнопками; чернильная тёмная с кремовыми поверхностями), Индиго (сине-фиолетовая на светлой канве и глубоком индиго). Палитры портированы из опубликованных дизайн-анализов под нейтральными цветовыми именами — брендовые названия и шрифты в продукт не входят.
- **Хардкодные серые заменены на переменные темы** — остатки `bg-white` / `gray-*` в «Автоматизации», статусе администратора и вкладке трекинга кампании используют `var(--color-*)`, поэтому любая тема, включая новые тёмные палитры, красит эти страницы корректно. Вкладка трекинга заодно ужата: панель «Настройки и опции» показывается только для методов с опциями, а блок сгенерированного кода стал полной ширины с компактной кнопкой копирования в заголовке.
- **Фиксы** — задвоенный «+ +» на кнопке «Подключить Cost Sync» (литеральный плюс в строке перевода рядом с иконкой); починен бандл страницы Conversion Types, сломанный с 1.1.2, — блок немапленных статусов из релизных нот 1.1.2 реально доехал до пользователей только сейчас.

## 🆕 Новое в v1.1.2

- **Лог входящих постбэков (ORB-001)** — каждый постбэк, приходящий в трекер, записывается как есть, до маппинга статусов и атрибуции. Пропавшая конверсия или конверсия с «не тем» статусом теперь прослеживается до конкретного запроса, который прислала сеть.
- **Немапленные статусы конверсий + ретроактивный маппинг** — движки Facebook и TikTok сначала смотрят кастомный маппинг статусов и только потом дефолтный; статус, для которого нет правила, сохраняется как unmapped, а не приводится к догадке. Новый блок в настройках Conversion Types показывает накопленные немапленные статусы и позволяет задать правило — оно применяется и задним числом, переклассифицируя уже сохранённые конверсии.
- **LeadForge больше не выдумывает subid (ORB-011)** — убран фолбэк `bin2hex(random_bytes(8))`, выдумывавший subid для лидов без контекста клика. Subid проверяется по таблице `clicks`: просроченный или отсутствующий — лид отклоняется с нейтральным сообщением для покупателя и записью в `system_logs`; при недоступной БД лид всё равно принимается, чтобы доступность не зависела от проверки.
- **Консолидация роутинга** — `router.php` удалён, `index.php` — единственный фронт-контроллер; в него перенесены оба guard'а: Disabled Domain (404 для выключенных доменов) и Admin Access (ограничение админских маршрутов по настройке домена). Маршрут постбэков и пиксель конверсий работают через него же, отдают валидный GIF с корректными кодами ошибок и покрыты real-HTTP-тестами (ORB-010).
- **Ассеты лендингов через X-Accel-Redirect (ORB-013)** — PHP проверяет путь к ассету и отдаёт файл nginx'у через `X-Accel-Redirect`: файл уходит sendfile'ом (zero-copy), PHP освобождается для следующего запроса — лендинг с 30 ассетами больше не занимает 30 PHP-воркеров. Внутренняя локация отдаёт только типы из белого списка, остальное (включая `.php`) — `deny all`. **На работающем nginx-сервере нужно один раз выполнить `sudo php /var/www/orbitra/cli/nginx_sync.php`** — половина изменения живёт в конфиге vhost. Apache-установкам ничего ручного не нужно.
- **Производительность клика и аналитики** — синхронные обращения к внешним geo-API убраны из пути клика (клик не ждёт сторонний ответ); на таблицу `clicks` добавлены покрывающие индексы под фильтры аналитики и трендов.
- **Фиксы** — редирект-луп за Cloudflare Flexible SSL (`orbitraIsHttps()` читает `CF-Visitor`); единый источник public IP сервера (ORB-005) вместо разрозненных проверок в каждой фиче; фрагмент-фикс в IntegrationsPage.

## 🆕 Новое в v1.1.1

- **POST формы локального оффера больше не отдаёт 404** — загруженный оффер отдаётся inline по адресу кампании (`/pr6sxv41`), поэтому браузер разрешал `action="order.php"` относительно корня домена и отправлял лид на `/order.php`. Этот путь первым забирал nginx: `snippets/fastcgi-php.conf` заканчивается на `try_files $fastcgi_script_name =404`, файла `order.php` в корне нет — 404 отдавал сам веб-сервер, и order-мост в `index.php` не запускался. Генератор конфига (`core/nginx_config.php`) и базовый конфиг из `install.sh` теперь передают `/order.php`, `/offers/<id>/*.php` и `/lander/<slug>/*.php` во фронт-контроллер. **На работающем сервере нужно один раз выполнить `sudo php /var/www/orbitra/cli/nginx_sync.php`** — половина этого исправления лежит в конфиге nginx, а не в коде.
- **`success.php` добавлен в список обработчиков** — сгенерированный `order.php` заканчивается относительным `Location: success.php`, поэтому лид мог быть принят партнёркой, а покупатель шагом позже всё равно получал 404. `orbitraBundleHandlers()` — единый список для всех трёх мостов: `order.php`, `thank_you.php`, `success.php`, `send.php`, `lucky.php`, `lemon.php`, плюс `api.php` только на маршрутах, где id бандла есть в самом URL, и никогда в корне домена, где это админский API трекера.
- **Action формы указывает на адрес самого оффера** — относительный `*.php` в отданном оффере становится `/offers/<id>/order.php`: POST лида несёт id оффера, а не полагается на cookie или Referer, и отправщик `api.php` из бандла Ezaff больше не конфликтует с `/api.php`. Анкоры `#order`, ассеты и любые абсолютные или внешние action не трогаются — намеренно без тега `<base>`, который превратил бы каждую кнопку «#order» в переход с адреса кампании.
- **Загруженный PHP больше не исполняется напрямую с диска** — `/offers/<id>/order.php` раньше уходил прямо в PHP-FPM, то есть файл из загруженного архива выполнялся в обход переключателя «Allow PHP landings» и лимита времени. Оба маршрута бандлов идут через `index.php`, а `/landings/<id>/*.php` (директория хранения за `/lander/<slug>/`, не публичный маршрут) отдаёт 404.
- **`ORBITRA_OFFER_ID` / `ORBITRA_OFFER_URL` / `ORBITRA_OFFER_PATH`** — объявляются до запуска любого собственного PHP локального оффера, на всех трёх путях его исполнения. Бандл может написать `defined('ORBITRA_OFFER_URL') ? ORBITRA_OFFER_URL . 'order.php' : 'order.php'` и остаться работоспособным вне Orbitra.
- **`tests/lander_order_route_test.php` вырос до 50 проверок** — `success.php` на обоих мостах, константы контекста оффера, `/api.php` никогда не перехватывается бандлом, двенадцать случаев перезаписи action и сверка того, что генератор vhost и `install.sh` описывают маршруты бандлов одинаково и ставят их перед общим PHP-обработчиком.

## 🆕 Новое в v1.1.0

- **Атрибуция конверсий из постбеков партнёрок** — конверсия, принятая через `postback.php`, теперь получает измерения своего клика: `campaign_id`, `offer_id`, `sub_id_1..5`, IP и user agent. Раньше заполнялся только `click_id`, поэтому журнал конверсий показывал строки без кампании и оффера, а фильтры по кампании/офферу в нём не находили ничего. Логика вынесена в `core/ConversionAttribution.php` и переиспользуется импортом конверсий из CSV.
- **Восстановлены слоистые отчёты** — в SQL-запросе `campaign_report` оказался комментарий вида `//`, которого SQLite не понимает (там допустимы только `--` и `/* */`). Запрос не парсился целиком, поэтому **любой** сгруппированный отчёт (Sub1…Sub30, Страна, День, Кампания, Оффер) отдавал ошибку вместо строк.
- **Регистронезависимое сопоставление статусов** — `Approved` или `PENDING` от партнёрки записывались как конверсия, не попадающая ни в одну группу статусов: в кампании было видно `Конверсии: 1` при нулевых Продажах, Лидах, Отклонённых и Трэше. Сравнение приведено к нижнему регистру и в `mapStatus()`, и в агрегатах отчётов; словарь дополнен значениями `approve/accepted/paid`, `new/wait/waiting/processing`, `reject/decline/cancelled` и `spam/duplicate`.
- **`subid` не попадает в измерение Sub1** — входящий `subid` это Click ID трекера (Dr. Cash передаёт его в `&sub1={subid}`), а sub-измерения конверсии берутся из `parameters_json` самого клика. Иначе каждая строка отчёта по Sub1 стала бы уникальной для одного клика.
- **Приоритет собственного правила статуса** — при одновременных `trash_status=trash` и `rejected_status=rejected,trash` статус `trash` остаётся Трэшем, а не уходит тому типу, который просто упомянул его первым.
- **Миграция 33** — дозаполняет `campaign_id`, `offer_id`, `sub_id_1..5`, IP и user agent у ранее записанных конверсий по их кликам. Строки, у которых клик уже удалён, не трогаются.
- **Мониторинг сбоев конверсий** — `core/ConversionMonitor.php` пишет причину, по которой конверсию не удалось создать (неизвестный клик, ошибка БД), в `var/logs/conversion_failures.log`; `api.php?action=conversion_monitoring` отдаёт количество и долю сбоев за окно, а `conversion_monitor_cron.php` (раз в 5–15 минут) шлёт оповещение в Telegram при превышении порога.
- **Выпуск SSL с учётом облака** — `core/CloudDetector.php` определяет домены за прокси Cloudflare по диапазонам IP и заголовкам ответа, чтобы не гонять Certbot туда, где он не пройдёт валидацию; `core/ssl_manager.php` проверяет DNS и passwordless sudo до запроса сертификата, а в «Доменах» появился перевыпуск с понятной причиной отказа. Подробности — в `CERTBOT_SETUP.md` и `SECURITY_OPERATIONS_CHECKLIST.md`.
- **Возвращена проверка TLS на исходящих запросах** — `telegram_notify.php`, `telegram_bot.php`, `postback_queue_cron.php` и `core/LeadForge.php` отправляли cURL-запросы с `CURLOPT_SSL_VERIFYPEER => false` и принимали любой сертификат. Проверка снова включена по умолчанию и ослабляется только при явном локальном окружении (`APP_ENV=local`, `ORBITRA_ENV=dev`, `ORBITRA_SKIP_SSL_VERIFY=1`).
- **Подключение расходов TikTok под API v1.3** — запрос `advertiser/info/` с массивом `advertiser_ids`, разбор Advertiser ID строго из цифр (вычищаются типографские кавычки, невидимые символы и пробелы из буфера обмена) и ошибки с указанием причины вместо голого кода. В подписи к полю токена указано, что нужен токен **Marketing API** с правами Ads Management / Reporting.
- **Руководство по 1-Click OAuth для Google Ads** — пошаговая настройка проекта Cloud, Redirect URI и Developer Token прямо в панели, CLI-проверка `check_google_ads_oauth.php` и описание переменных в `.env.example`.
- **Регрессионный тест `tests/postback_attribution_test.php`** — путь Dr. Cash целиком: атрибуция, повторный постбек без перезаписи, бэкфилл, принадлежность статуса ровно одной группе, подсчёт статусов в разном регистре и строка отчёта по Sub1 с кликами и конверсиями вместе.

## 🆕 Новое в v1.0.9

- **Универсальное разрешение макросов в локальных PHP-лендингах и офферах** — перехват буфера вывода (`ob_start()`) с автоматической подстановкой всех макросов (`{subid}`, `{sub1}`, `{click_id}`, `{{subid}}`, `{data1}`, `{external_id}` и др.) перед отдачей клиенту, установка сквозных cookie `orbitra_click`, `orbitra_subid`, `subid`, `orbitra_lo`.
- **Отказоустойчивая обработка лидов в `order.php`** — многоуровневый каскад извлечения идентификатора клика (куки, сессии, параметры URL, сгенерированный ID), гарантирующий корректную отправку лидов в CPA-сети даже при отправке непрорезолвленного макроса.
- **Интерактивные руководства и справочники по ключам интеграций** — добавлены подробные пошаговые инструкции по подключению расходов для **Google Ads** (Customer ID, MCC Developer Token, Client ID/Secret, Refresh Token), **TikTok for Business** (Advertiser ID, Long-term Access Token, App ID/Secret) и **Facebook / Meta** (System User Token, Ad Account ID).
- **Интерактивные карточки Authorized Redirect URI** — выведены готовые ссылки обратного вызова для Google Cloud Console, TikTok Developer Portal и Meta Developers с копированием в один клик и подсказками по настройке.
- **Плавный fallback UX для 1-Click OAuth** — при отсутствии настроенных ключей приложения клик по кнопке входа автоматически переключает пользователя на прямой ввод токенов (Manual режим) с разворачиванием руководства.
- **100% паритет локализации во всех 7 языках** — полные переводы всех инструкций, подсказок и ключей добавлены в `ru`, `en`, `uk`, `de`, `es`, `fr`, `zh`.

## 🆕 Новое в v1.0.8

- **Эталонная синхронизация LeadForge 2.0 и база на 150 ГЕО** — полная интеграция датасета валидации для 150 стран (`core/data/leadforge_geo_rules.json`) с точными национальными и международными regex-паттернами, префиксами мобильных операторов, ограничениями длины (min/max), телефонными кодами и сообщениями валидатора на 33 языках (включая СНГ, Индию, Европу, Латам, Арабские страны и Азию).
- **Интерактивный JS-движок валидации (`orbitra_adapter.js`)** — генератор клиентского валидатора переведён на эталонный шаблон (`core/data/leadforge_validation_template.js`):
  - *Динамическое переключение страны*: при смене `<select name="country">` маска, правила валидации и счётчик мгновенно перестраиваются под выбранное ГЕО.
  - *Интерактивный бейдж-счётчик*: живой визуальный индикатор введённых и недостающих цифр (*«3 cifre inserite, 7 mancanti»* → *«Numero complete»*).
  - *Строгая Unicode-валидация имени*: защита от ввода цифр и спам-символов в полях имени в реальном времени.
  - *Виброотклик*: тактильная вибрация мобильного устройства при некорректном вводе номера.
  - *Сквозной ClickID / SubID мост*: автоматический перенос и гидратация всех скрытых полей (`subid`, `click_id`, `sub1`..`sub5`, `utm_*`, `fbp`, `fbc`, `pub_sub_id` и др.) с сохранением в `sessionStorage` и cookie.
- **Универсальный CPA Order Bridge (`order.php`)** — нативная поддержка 10 партнёрских сетей с прямыми эндпоинтами и авторизацией:
  - *Dr.Cash*: `https://order.drcash.sh/v1/order` (Bearer token, `sub1`..`sub5`).
  - *Webvork*: `https://api.webvork.com/v1/new-lead` & `api2` fallback (`utm_campaign` = `{subid}`).
  - *Lucky.online*: `https://lucky.online/api/v1/lead-create/webmaster` (`campaign_hash`, `subid1`/`subid`).
  - *KMA.biz*: `https://api.kma.biz/lead/add` (Bearer token, `data1`..`data5`).
  - *TerraLeads*: `https://t-api.org/api/lead/create` (SHA1-хеширование `json + api_key`).
  - *Leadbit*: `http://wapi.leadbit.com/api/pub/new-order/{token}` (`flow_hash`, `sub1`..`sub5`).
  - *LemonAD*: `https://lemonad.com/api/v2/lead/create` (`click_id`).
  - *Everad*: `https://api.everad.com/campaigns/{offer_id}/order` (`X-Api-Key`, `sid1`).
  - *Ezaff*: `https://api.ezaff.com/send` (`click_id`, `pub_sub_id`).
  - *Custom Webhooks*: сквозной POST-проброс на кастомные CRM и API.
- **Автоматическая нормализация телефонов в E.164 (`lf_normalize_phone`)** — надёжное приведение локальных номеров к международному стандарту с учётом национальных правил и отсечением лидирующих нулей.
- **Dual Logging и отказоустойчивый CRM Vault** — одновременная отправка лида в сеть и запись в CRM Vault трекера (`orbitraCrmRecordLead` / `/crm-ingest`) + локальные резервные логи (`leadforge.leads.log` и `orbitra_leads_backup.log`).
- **Автоматизированный тестовый комплекс (`tests/leadforge_sync_test.php`)** — полный цикл проверок 150 ГЕО, генерации JS-адаптера, сборки order.php под 10 сетей и изоляции маршрутизации.

## 🆕 Новое в v1.0.7

- **Современный Hub интеграций (Grid / Card Hub)** — страница интеграций (`IntegrationsPage.jsx`) переведена на модульную двухуровневую плиточную архитектуру в стиле современных платформ (Vercel, Stripe). Убран тесный вложенный двойной сайдбар, 25 сервисов сгруппированы по 4 категориям: «Реклама и Расходы», «Домены и SSL», «Трекинг и Сайты», «Инструменты и API».
- **Живые индикаторы активности и параллельный префетчинг** — карточки отображают анимированные индикаторы подключения, живые балансы Namecheap, счетчики активных зон Cloudflare, статус Telegram бота и сгенерированные API-ключи. Функция `loadOverviewData` загружает актуальные метрики параллельно при монтировании компонента.
- **100% ширина экрана в детальном режиме** — выбор любой интеграции открывает просторный полноэкранный интерфейс без сжатия боковыми меню, с верхней навигационной панелью (`← Назад ко всем интеграциям`), хлебными крошками и бейджем активности.
- **Встроенная IDE и файловый менеджер локальных офферов (`OfferEditor.jsx`)** — редактирование прямых локальных офферов (`is_local=1`) получило полноценную браузерную среду разработки: интерактивное дерево файлов с подсветкой синтаксических типов, редактор кода в стиле Monaco (HTML, CSS, JS, PHP, JSON, XML) с поиском и заменой (`Ctrl/Cmd+F`), форматированием, живым предпросмотром на десктопе и мобильном (iframe), предпросмотром графики, загрузкой, заменой и удалением файлов.
- **Безопасное API файловых операций (`api.php`)** — эндпоинты `offer_files`, `offer_file_content`, `save_offer_file`, `offer_file_op`, `upload_offer_file` со строгой изоляцией `realpath` против path traversal, многоуровневым PHP-сканированием и автоматическими тестами `tests/offer_file_ops_test.php`.
- **100% паритет 7 языков** — полная локализация всех новых элементов на английский (`en`), русский (`ru`), немецкий (`de`), испанский (`es`), французский (`fr`), украинский (`uk`) и китайский (`zh`).

## 🆕 Новое в v1.0.6

- **Мультиаккаунт Cloudflare (миграция 32)** — легаси-токен `cf_api_token` становится аккаунтом №1 в новой таблице `cloudflare_accounts`, домены получают `dns_account_id`: страница Domains выбирает аккаунт для каждого домена, Integrations управляет аккаунтами (добавить / редактировать / удалить, per-account SSL-режим и proxied, импорт зон и repoint через API).
- **Мультиаккаунт Namecheap (миграция 31)** — зеркало для регистратора: легаси `nc_api_key` становится аккаунтом №1 в `namecheap_accounts`; Integrations показывает аккаунты с живой проверкой баланса, добавление / переименование / удаление.
- **OAuth-префлайн TikTok и Google Ads** — `<engine>_oauth_status` как у Facebook: без настроенного приложения кнопка 1-Click отключается с янтарной карточкой вместо падения посреди флоу; каждая сеть принимает и прямое подключение токеном (ручной режим — по умолчанию для новых кредов).
- **Delete guard для занятых лендингов и офферов** — удаление сущности, на которую ссылается поток активной кампании, отклоняется с `entity_in_use` и списком кампаний; фронт локализует отказ (`utils/entityInUseError.js`) вместо трактовки HTTP-200-ошибки как успеха.
- **Суб-часовая синхронизация расходов** — `sync_interval_hours` принимает дробные значения (0.5 / 0.333) для почти realtime-тиков агрегатора.
- **Контраст кастомной темы** — выбранный primary сам вычисляет инверсный цвет текста по воспринимаемой яркости (неоновый лайм получает тёмный текст, а не нечитаемый белый); выход из кастомной темы снимает inline-оверрайд.
- **Фидбек копирования в модалке API-ключей** — кнопки копирования URL-коннектора и ключа меняются на зелёную галочку на 2 секунды и подтверждают баннером внутри модалки; нотисы генерации/удаления ключей переехали туда же (старый тост рендерился под оверлеем и выглядел как «кнопка не работает»).
- **Ужесточение загрузки локальных офферов** — двухуровневый PHP-скан (жёсткий блок опасных конструкций, мягкая санитизация остального), закрыта дыра `T_NAME_FULLY_QUALIFIED` (неймспейс-вызовы были невидимы для сканера), архивы попадают через stage-then-swap с realpath-контейнментом — симлинки не выводят за пределы диры оффера; мост order.php/thank_you.php обслуживается с той же проверкой realpath.
- **Chrome-расширение 1.0.1** — RU/EN локализация live-оверлея, бейдж «Черновик · ждём трафика», MutationObserver игнорирует собственные инъекции расширения (ре-рендер больше не плодит циклы обновлений); пересобран orbitra-extension.zip.

## 🆕 Новое в v1.0.5

- **Safe Page: группировка белых страниц** — селектор вайта в блоке клоакинга (Tracker Landing) собирается через `<optgroup>` по названию группы лендинга (группы по алфавиту, «Без группы» — в конце): библиотека из десятков вайтов остаётся навигируемой. Рядом — кнопка 🔍: открывает `EntitySelectorModal` в новом режиме одиночного выбора (клик по строке = выбор; поиск и фильтр по группе уже были) — работает и для лендингов, и для локальных офферов.
- **Safe Page: «Локальный оффер» как вайт** — четвёртая вкладка в блоке Safe Page (External URL | Tracker Landing | Local Offer | Inline HTML): выбор среди локальных офферов (`is_local=1`, включая собранные LeadForge в режиме «Лок. оффер») с той же группировкой. Сохраняется как `safe_mode='offer'` + `safe_offer_id`; легаси-схемы без `safe_mode` резолвятся по наличию полей. Все три точки входа синхронны: index.php отдаёт архив инлайн с полным контекстом (макросы, PHP-страницы, кука `orbitra_lo`), click.php и Click API редиректят на публичный `/offers/<id>/`; несуществующий или не-local оффер — фолбэк на дефолтную страницу, как у безопасного лендинга.
- **Публичный роут `/offers/<id>/`** — «близнец» `/lander/<slug>/` для локальных офферов: отдаёт index загруженного архива с инъекцией `<base>` и установкой куки `orbitra_lo` (относительные ассеты и мост order.php продолжают работать), 404 для несуществующих/архивных офферов, PHP-индексы не исполняет, не-страничные файлы идут через тот же whitelist расширений и containment, что и в клик-потоке.
- **Фикс: direct local offer «URL not found.»** — `die()` при пустом `$finalUrl` срабатывал раньше ветки раздачи локального оффера, поэтому «прямой локальный оффер» из v1.0.4 для потоков без лендинга фактически не работал. Теперь страница отдаётся как задумано; `null`-url больше не даёт PHP 8.5-deprecation на `str_replace`; Click API отвечает адресом `/offers/<id>/` на money-стороне cloak-схемы и в redirect-схеме.
- **Фикс: сид `allow_php_landings` при обновлении** — строки-дефолты из v1.0.4 добавили без бампа схемы, и базы, уже стоявшие на схеме 29 (ставились с 1.0.2), их не получили → LeadForge падал с `php_landings_disabled`. Миграция 30 вставляет `allow_php_landings`/`php_landing_timeout` (`INSERT OR IGNORE`, явный `'0'` выживает), `PhpLanding::enabled()` считает отсутствующую строку включённой; Settings → General и панель LeadForge используют те же семантики default-on. Тест `tests/settings_seed_v30_test.php` гоняет настоящий блок миграции, извлечённый из config.php.

## 🆕 Новое в v1.0.4

- **LeadForge: назначение в трекере (Tracker destination)** — сегментированный контрол в панели сборки: «Не сохранять / ZIP only», «Лендинг» (в Landings), «Лок. оффер» (прямой локальный оффер: запись в Offers с `is_local=1`, файлы копируются в `/offers/<id>/` и раздаются штатным роутом локальных офферов через куку `orbitra_lo`, записи лендинга не создаётся), «Лендинг + оффер» (связка). Старая пара флагов `auto_save_tracker`/`auto_create_offer` продолжает работать для API-клиентов без `target_type`. Live Auto QA для прямых офферов пропускается с честным логом (нет записи лендинга).
- **Группы на лету в LeadForge** — селектор группы переключается между группами лендингов и группами офферов по типу назначения; «+ Новая группа» создаёт её инлайн (Enter/кнопка), сразу выбирается; дубль имени — рефетч и выбор существующей. `new_group_name` в API резолвится один раз на батч (защита от `UNIQUE(name)`), с фолбэком на существующий id.
- **Opt-in фиксированная выплата** — выплата не вшивается в сборку по умолчанию; реальный доход — S2S-постбек сети (`postback.php` апсертит выплату по конверсии), галка «Фиксированная выплата» включает ручное значение.
- **Фикс: календарь диапазона дат за экраном** — `DateRangePicker` умеет `align` (`'left'|'right'|'auto'`, по умолчанию `auto`): при открытии (и на resize) измеряет вьюпорт и открывается в сторону, где влезает 540px панель; `maxWidth: calc(100vw - 24px)` как страховка. Landings чинится, Campaigns/Offers/CampaignReports не меняются.
- **Фикс: FK групп офферов** — `offers.group_id` связан FK с `offer_groups`, а код писал туда id группы лендингов → при `PRAGMA foreign_keys=ON` сборка с группой падала; теперь ищется одноимённая группа офферов, нет — NULL. Заодно POST `landing_groups` с дублём имени возвращает ошибку, а не 500.
- **Префла Facebook OAuth** — action `facebook_oauth_status` отдаёт, настроено ли Meta-приложение (env/settings/ручное подключение); кнопка 1-Click дизейблится с предупреждением вместо гарантированно неудачного попапа; добавлен сворачиваемый блок «где взять токен».
- **PHP-лендинги включены по умолчанию** — `allow_php_landings=1` (бандлы LeadForge живут на order.php/thank_you.php и без этого не работают на чистой установке); тумблер и таймаут вынесены в Settings → General (обе части whitelist в API уже были).

## 🆕 Новое в v1.0.3

- **Find & Replace в редакторе кода лендингов** — виджет в стиле VS Code над textarea: `Ctrl/Cmd+F` поиск, `Ctrl/Cmd+H` замена, модификаторы регистра / целого слова / regex, живой счётчик `N из M`, навигация Enter/Shift+Enter с прокруткой к совпадению (точная вертикальная математика по фиксированной высоте строки + измеренный горизонтальный скролл для минифицированных однострочников), одиночная Replace и посчитанная Replace All (литеральный режим сшивает список совпадений — пустые regex-матчи не размазывают замену между символами), запрос предзаполняется из выделения в редакторе.
- **CRM и LeadForge на всю ширину** — снят `max-w-7xl` (единственные две страницы с ограничением 1280px), теперь единообразно с дашбордом; App.jsx больше не рендерит дубль заголовка над их hero-шапками.
- **CRM-полировка** — кнопки копирования SubID и телефона по ховеру строки (галка на 1.5с); Approval Rate переехал под карточку Approved Sales; карточка Shave Suspects получила розовую подложку и показывает только lost-in-transit.
- **79 ГЕО в LeadForge** — выбор страны вырос с 13 до 79, сгруппирован по регионам (Европа / Америки / Азия / Ближний Восток и Африка) через `optgroup` с переведёнными названиями групп; `geoMasks()` в `core/LeadForge.php` — источник истины, расширен 66 мобильными масками (паттерн + min/max по реальным форматам); маски читают генерация order.php, валидация адаптера и скоринг Live Auto QA. Синхронность списков frontend ↔ backend проверяется скриптом.
- **Фикс: coexistence API-ключа и сессии** — ключ на запросе из браузера с активной сессией панели принимается только при совпадении пользователя (иначе 403); живая сессия не переидентифицируется как `api_key`; невалидный ключ даёт 401 даже при наличии сессии.

## 🆕 Новое в v1.0.2

- **LeadForge 2.0: Analyze → Build** — двухэтапный конвейер вместо одношотового forge: `leadforge_analyze` инспектирует до 15 ZIP/HTML/PHP бандлов без изменения файлов (детект источника по 13 сигнатурам CPA-сетей, формы и имена полей, чужие счётчики, проверка UTF-8, ГЕО по атрибуту `lang`, карточки готовности с чекбоксами) и держит staging 24 часа; `leadforge_build_batch` собирает выбранные бандлы с живой консолью исполнения. Одношотовый `leadforge_forge_landing` сохранён как делегат того же движка.
- **Режимы интеграции Auto / Cross-Network / Raw** — Auto маршрутизирует по опознанной сети; Cross вырезает легаси-обработчики старой ПП (`order.php`/`send.php`/`api.php`/`sender.php`) и пересаживает лендинг на целевую; Raw — клон-патч: снос FB/TikTok/GA/Яндекс-счётчиков, DevTools/RightClick/Back-Redirect-скриптов, инъекция ClickID-моста и макросов `{offer}` без генерации бэкенда.
- **Live Auto QA с Confidence Score** — после сборки `QA-Test-Lead` с телефоном по маске ГЕО (для IT — `+39 333 000 1122`) прогоняется через реальный мост `/order.php` (cookie `orbitra_lp`); 4 чека по 25% (структура формы, ответ моста, dual logging, редирект на thank_you) дают 0–100%; перепрогон — `leadforge_live_qa`. QA-клики и конверсии удаляются после прогона — аналитика чистая, vault-строка остаётся как проверяемое доказательство.
- **CRM Anti-Shaving Vault (миграция 28)** — таблица `crm_leads`: сырой телефон как ввёл клиент vs доставленный E.164, атрибуция UTM/adset/ad, product/price, точный дамп запроса/ответа сети (endpoint, payload, HTTP-код, тело, network lead id), status_reason/source. Три пути приёма: in-process из сгенерированного order.php, публичный `POST /crm-ingest` для бандлов на чужом хостинге, вручную из панели. `leadforge_profiles` засеяна шестью сетями движка.
- **S2S-сверка и детектор шейва** — постбек двигает CRM-статусы клика (`&reason=` сохраняется дословно); **Suspected Shave** ставится только при доказуемой связке «доставлен корректный E.164 + сеть ответила HTTP 200 + реджект»; молчание сети 24 часа после 200 — **Missing Network ACK**; тот же номер в той же сети за 30 дней — **Duplicate**. Lead Inspector: 3 таба доказательств (Raw Lead Data / Network Transaction / Tracking Attribution) с экспортируемым пакетом для тикета в саппорт ПП.
- **orbitra_adapter.js 2.0** — ClickID-мост читает cookie трекера `orbitra_click` как последний резерв subid, принимает алиасы `clickid`/`sub_id`, хранит захваченные параметры между страницами (sessionStorage + cookie) и принудительно применяет реальную маску телефона ГЕО (placeholder, фильтрация ввода, блокировка сабмита).
- **Фиксы** — мост order.php умирал от 3-секундного бюджета PHP-лендингов посреди сетевого вызова (теперь `max(timeout, 25с)`); router.php на dev-сервере глотал `/postback.php`, `/order.php`, `/crm-ingest` пустым 200 (список зарезервированных путей синхронизирован с index.php); LeadForge и CRM переведены на глобальную систему тем (btn-primary/btn-secondary, color-mix-тинты, `--color-text-inverse` на primary для neon), бейджи «3-in-1» убраны.

## 🆕 Новое в v1.0.1

- **1-Click TikTok For Business** — OAuth-вход попапом из Pixel Vault (api.php `tiktok_oauth_start/callback/connect_accounts`, зеркало Facebook-флоу): авто-обнаружение кабинетов (`/oauth2/advertiser/get/`) и пикселей (`/pixel/list/`), чекбоксы кабинетов с интервалом синка расходов (движок `tiktok`), авто-импорт пикселей в хранилище; токен не покидает сервер (во попапе только flow_id).
- **Авто-продление TikTok-токена** — access-токен TikTok живёт ~24 часа, refresh-токен одноразовый (год); `TikTokAdsEngine::ensureFreshToken()` продлевает токен на каждом тике синка расходов (крон и «Sync now»), обновляет все копии подключения того же логина и протягивает новый токен в pixel_profiles/campaign_pixels по совпадению старого; мёртвый refresh-токен роняет синк с внятной ошибкой.
- **Табы источников в Pixel Vault** — чипы All / Facebook / TikTok / Google Ads… со счётчиками; в селекторе пикселя редактора кампании опции вида `pixel_id ( TikTok · Ниша / Имя )`.
- **Сниппет TikTok Pixel** — Integrations → Sites & landings: cookie из параметра кампании `{pixel}` (шаблон источника TikTok, `__PIXEL__`) + загрузчик `ttq` для лендинга и thank-you.
- **Фиксы** — белый экран Campaigns (TDZ стейта фильтра источников из v1.0.0), свечение активных карточек метрик через color-mix + отступ над Traffic Dynamics, поиск в Traffic Sources (иконка, крестик, стили карточки).

## 🆕 Новое в v1.0.0

- **LeadForge + CRM-капсула** — генератор готовых к заливке лендинг-пакетов: order.php/thank_you.php обслуживаются встроенным мостом index.php (работает даже на статическом хостинге), subid передаётся hidden-полем или cookie orbitra_click, готовые пакеты скачиваются по токенизированной ссылке; лиды собираются через `crm_lead` (создаёт минимальный клик, если его нет); переключатель LeadForge / Tracker / CRM в шапке.
- **TikTok Conversions API** — серверные события через core/TikTokConversions.php рядом с Facebook CAPI; пиксель-профили (источник, ниша, набор событий, тест-коды) управляют обоими; схема БД v26, панель миграций (schema_migrations).
- **Инлайн-поиск** в Offers / Campaigns / Landings — инпут в группе действий шапки рядом с Create/Groups; живой фильтр по имени, URL, группе, сети и точному ID; крестик быстрой очистки.
- **Универсальная пагинация таблиц** — общий PaginationToolbar: Showing X-Y of N · Page Size 25/50/100/250/All (запоминается в localStorage для всех таблиц) · First/Prev/номера/Next/Last; строка TOTAL считается по всему фильтру, а не по странице.
- **Адаптив** — шапка переходит на брейкпоинт xl (1280px) с компактными пунктами и полной шторкой ниже него (фикс обрезания на ноутах 768–1280px); дашборд без горизонтального скролла — карточки метрик сеткой 2→7 колонок, селекторы кампании/дат резиновые.
- **Фильтр по источникам в Campaigns** — селектор «Все источники трафика» рядом с группами; мгновенный клиентский фильтр (строки уже несут source_id/source_name), TOTAL пересчитывается.
- **Веса потоков** — при ротации By weight в шапке каждого потока поле веса и живой бейдж доли (70.0%); панель суммарного веса с предупреждением при ≠100% и кнопкой «Разделить поровну».
- **Оптимизация Traffic Sources** — статистика источников одним подзапросом (без полного JOIN clicks), кэш шаблонов редактора на уровне модуля.

## 🆕 Новое в v0.9.9.2

- **Play/Pause** — пауза внутренних кампаний и реальных объявлений/адсетов Facebook прямо из таблиц и отчётов (Meta Marketing API).
- **«Собирать клики» на уровне потока** — фолбэк/white-page потоки не пишут клики в статистику (миграция 24).
- **Панель доменов Keitaro-style** — группы доменов, запрет админки на домене (404), Cloudflare-прокси (пропуск Certbot), регистратор/DNS-метаданные, статус Disabled, массовое добавление с чисткой URL (миграция 23).
- **Расширение для Facebook Ads Manager** — оверлей с прибылью/ROI/CPA в строках и модалкой: история по дням, лендинги/офферы, точность Pixel/CAPI. Ключ читается из Integrations, zip — `data/orbitra-extension.zip`.
- **Плотные таблицы** — 38px строки, sticky-шапка и TOTAL, зебра, ⋮-меню (в т.ч. Copy link и Duplicate), пагинация 25/50/100/All; все деньги в формате $0.00 и в CSV.
- **Таксономия устройств** (Десктоп/Мобильные/Планшеты) общая для трекера, Click API и отчётов; метрика CPV; пресеты настроек редактора; шаблоны партнёрских сетей.

## 🎯 Управление кампаниями

### CampaignEditor — полноэкранный редактор кампаний

Редактор кампаний (`CampaignEditor.jsx`, ~130KB) спроектирован для максимального удобства и вдохновлён интерфейсом Keitaro.

#### Вкладка "Основные"
- **Название кампании** — произвольное имя
- **Алиас** — авто-генерация 6 символов (keitaro-style)
- **URL кампании** — автоматически копируется в буфер
- **Домен** — выбор из добавленных доменов
- **Группа кампаний** — для организации
- **Источник трафика** — привязка к источнику
- **Код для лендингов** — JavaScript код для вставки на сайты

#### Вкладка "Финансы"
**6 моделей вознаграждения:**
- **CPC** (Cost Per Click) — оплата за клик
- **CPuC** (Cost Per Unique Click) — оплата за уникальный клик
- **CPM** (Cost Per Mille) — оплата за 1000 показов
- **CPA** (Cost Per Action) — оплата за действие (lead)
- **CPS** (Cost Per Sale) — оплата за продажу
- **RevShare** — процент от дохода партнёрки

**Настройки уникальности:**
- **Метод**: IP, IP+UA, Cookies
- **Срок уникальности**: 1-168 часов

**Ротация потоков:**
- **По весу** — пропорциональное распределение трафика
- **По позиции** — последовательное распределение

#### Вкладка "Параметры"
**30+ параметров** для передачи в оффер/лендинг:
- `keyword` — ключевые слова
- `cost` — стоимость клика
- `currency` — валюта
- `external_id` — внешний ID
- `creative_id` — ID креатива
- `ad_campaign_id` — ID рекламной кампании
- `source` — источник трафика
- `sub_id_1` ... `sub_id_30` — кастомные sub ID

#### Вкладка "Интеграции"
**5 предустановленных интеграций:**
- 📘 **Facebook** — автоматические параметры (fbclid, adset_id, campaign_id)
- 🎵 **TikTok** — параметры (ttclid, campaign_id, adgroup_id)
- 🔍 **Яндекс** — параметры (yclid, campaign_id, ad_id)
- 🔎 **Google Ads** — параметры (gclid, campaign_id, adgroup_id, creative)
- 💬 **VK Ads** — параметры (vk_click_id, vk_source_id)

#### Вкладка "S2S Postbacks"
- **URL для постбеков** — шаблон URL для отправки в партнерскую сеть
- **Метод** — GET или POST
- **Статусы** — выбор статусов для отправки (lead, sale, rejected, etc.)

#### Вкладка "Заметки"
- Текстовое поле для заметок к кампании

#### Дополнительные действия (кнопка "⋮")
- **Полный отчет (CSV)** — экспорт статистики в CSV
- **Лог конверсий** — просмотр конверсий по кампании
- **Симуляция трафика** — тестирование потоков
- **Обновить расходы** — ручное обновление стоимостей
- **Очистить статистику** — сброс статистики кампании

### Потоки (Streams)

**3 типа потоков:**
1. **Intercept** — перехват (высший приоритет, для особых случаев)
2. **Regular** — обычный поток (основной трафик)
3. **Fallback** — замыкающий (резервный поток)

**Фильтры потоков:**
- **Страна** — выбор стран (включая профили: EU, CIS, Americas)
- **Устройство** — desktop, mobile, tablet
- **ОС** — Windows, macOS, Linux, Android, iOS
- **Браузер** — Chrome, Safari, Firefox, Edge, Opera
- **ISP** — провайдер (регулярка)
- **IP** — белый/черный список
- **Keyword** — ключевые слова
- **Referer** — реферер (регулярка)
- **Время** — часы работы
- **День недели** — дни недели

**Схемы потоков:**
- **URL** — редирект на внешний URL
- **Offer** — редирект на оффер
- **Landing** — показ локального лендинга
- **Landing + Offer** — показ лендинга со ссылкой на оффер (оффер опционален)
- **Action** — выполнение действия (404, 200 OK, etc.)
- **Cloak** — cloaking: подозрительным посетителям (боты, модераторы, дата-центр/VPN) показывается белая страница, реальным людям — денежная (см. ниже «Cloaking»)

### 🔀 Типы редиректа (с версии 0.9.6.0)

Каждый оффер имеет поле `redirect_type`, которое определяет, **как именно** посетитель отправляется к URL оффера. До 0.9.6.0 это поле хранилось, но в рантайме всегда отдавался HTTP 302. Теперь поддерживается:

| Тип | Поведение | Когда использовать |
|---|---|---|
| `redirect` | HTTP 302 (по умолчанию) | Большинство случаев — быстро и прозрачно |
| `js` | HTML со `<script>window.location` | Обходит блокировки серверного редиректа, сохраняет referrer |
| `meta_refresh` | `<meta http-equiv="refresh">` | Максимальная совместимость со старыми браузерами |
| `frame`/`iframe` | Оффер в полноэкранном `<iframe>` | Посетитель остаётся на вашем домене |
| `form_submit` | Авто-сабмит POST-формы | Данные трекинга в теле запроса, а не в URL |
| `curl_proxy` | Удалённая страница через ваш сервер + `<base>` | Контент под вашим доменом |

Макросы `{clickid}`, `{offer_id}`, `{sub_id_1..30}` и SSRF-проверка применяются ко всем типам одинаково.

### 🕵️ Cloaking (с версии 0.9.6.0)

Схема потока **Cloak** разделяет трафик на две ветки:

- **Safe page (белая)** — для ботов, модераторов, дата-центров/VPN. Настраивается: лендинг, URL или встроенный HTML.
- **Money page (денежная)** — для реальных людей. Работает как схема «Лендинг + Оффер».

**Слои определения** (`core/CloakDetector.php`):
- **Datacenter/hosting** — совпадение ASN по списку `core/data/asn_blocklist.json` (AWS/GCP/Azure/Hetzner/OVH и др.) + ключевые слова в ISP.
- **VPN/proxy** — список известных VPN-ASN.
- **Боты** — переиспользует существующие таблицы `bot_ips`/`bot_signatures`.
- **UA-эвристики** — отсутствие User-Agent, curl/python/headless/googlebot и т.п., отсутствие `Accept-Language`.
- **JS-проверка (опционально, по умолчанию выключена)** — активный слой. Посетитель сначала получает белую страницу и переходит на денежную только после выполнения JavaScript по одноразовой ссылке с HMAC-подписью и TTL 5 минут. Явный `navigator.webdriver=true` остаётся на белой странице. Ограничения приватности, пустой список плагинов, canvas и фоновые вкладки не считаются бот-сигналами, потому что они встречаются у обычных пользователей. Всё, что не исполняет JS, остаётся на белой странице. Цена — один дополнительный переход для каждого живого посетителя, поэтому слой включается отдельным флажком.

**Чувствительность** — сигналы делятся на «жёсткие» (попадание в блок-лист ASN/ботов, отсутствие UA, UA краулера или консольной утилиты) и «мягкие» (hosting-ключевое слово в ISP, отсутствие `Accept-Language`):

| Уровень | Срабатывает |
|---|---|
| **Low** | только жёсткий сигнал |
| **Medium** (по умолчанию) | жёсткий сигнал или два мягких |
| **High** | любой одиночный сигнал |

Список ASN редактируется вручную в `core/data/asn_blocklist.json`.

### Локальные лендинги: загрузка ZIP и пути к файлам

Архив распаковывается в `landings/<ID лендинга>/`, точкой входа считается `index.html` (или `index.php`, если файл положен на сервер вручную — в загружаемом ZIP PHP запрещён). Сама страница отдаётся по URL кампании, а не по `/landings/<ID>/`, поэтому относительные пути внутри лендинга браузер запрашивает от корня домена.

С версии 0.9.6.2 трекер разбирает такие запросы сам: он помнит показанный лендинг (cookie `orbitra_lp`) и отдаёт файл из его папки. Правки в разметке не нужны — архив, который открывается локально двойным кликом, работает в трекере как есть. Поддержаны относительные пути, `srcset`, `url()` внутри стилей, байтовые диапазоны для `<video>`, `ETag`/`304` и кеширование.

Что стоит знать:

- Абсолютные пути (`/img/hero.jpg`) и относительные (`img/hero.jpg`) работают одинаково.
- Ссылки на внешние домены остаются как есть — трекер их не проксирует.
- Отдаются только медиа и статика по списку расширений; `.php`, `.htaccess` и прочее из папки лендинга наружу не уходит.
- При повторной загрузке архива в тот же лендинг ID сохраняется, кеш в браузере живёт час.

### Ссылка на оффер: макрос `{offer}` (с версии 0.9.6.4)

Самый простой способ — оформить кнопку макросом, как в Keitaro:

```html
<a href="{offer}">Купить</a>
```

Трекер подставит вместо `{offer}` URL оффера, который выбрал поток, — уже с `{clickid}` и остальными параметрами внутри.

**Если офферов на странице несколько**, выбор должен идти через трекер, а не дописыванием параметра к `{offer}`:

```html
<a href="/?_lp=1&offer_id=10">Оффер 10</a>
<a href="/?_lp=1&offer_id=22">Оффер 22</a>
```

`{offer}` разворачивается в прямую ссылку рекламодателя, поэтому `{offer}&offer_id=10` просто допишет лишний параметр к чужому URL и оффер не переключит. По `/?_lp=1&offer_id=N` трекер выберет нужный оффер и перезапишет `offer_id` у клика, чтобы конверсия легла на него.

В HTML локального лендинга подставляются: `{offer}`, `{offer_id}`, `{clickid}`, `{subid}`, а также все параметры клика — `{keyword}`, `{sub_id_1}` … `{sub_id_30}` и прочие, пойманные шаблоном источника. Значения из URL экранируются, поэтому чужой параметр не сможет вставить свой тег.

Ничего, кроме этих макросов, трекер не трогает: `{unknown}`, JS-шаблоны, синтаксис Vue и Angular остаются как есть.

Если в потоке нет оффера, `{offer}` превращается в `/?_lp=1` — по клику будет понятное сообщение, а не пустая ссылка.

> В лендинге на PHP (`index.php`) макросы не накладываются — там доступны переменные `$clickId` и `$offerUrlMacros`.

### Переход с лендинга на оффер (`/?_lp=1`)

Для локальных лендингов в схеме «Лендинг + Оффер» переход на оффер делается ссылкой `/?_lp=1` (совместимо с Keitaro). Оформите кнопку оффера на странице лендинга так:

```html
<a href="/?_lp=1">Перейти к офферу</a>
```

При клике трекер находит оффер, привязанный к клику посетителя (запоминается через cookie при показе лендинга), подставляет макросы (`{clickid}`, `{sub_id_1}` … `{sub_id_30}`, `{offer_id}` и др.) и делает редирект на оффер. Чтобы указать конкретный оффер из потока, добавьте его ID:

```html
<a href="/?_lp=1&offer_id=10">Оффер 10</a>
```

Если в потоке выбран только лендинг без оффера, лендинг открывается как финальная страница, а ссылка `/?_lp=1` ничего не делает (оффера нет).

## 🔗 Click API с токенами

### Что такое Click API?

Click API — это механизм для работы с интеграционными скриптами, когда вместо `campaign_id` используется уникальный токен кампании.

### Преимущества токенов
- **Совместимость с Keitaro** — существующие скрипты продолжат работать
- **Безопасность** — токен сложно угадать, ID легко перебрать
- **Гибкость** — можно менять кампании без изменения скриптов

### Как это работает

**Традиционный подход:**
```
https://tracker.com/click.php?campaign_id=123
```

**С токеном:**
```
https://tracker.com/click.php?token=abc123def456...
```

### Генерация токена

- **Длина**: 32 символа (hex)
- **Уникальность**: автоматическая генерация при создании кампании
- **Регенерация**: можно сгенерировать новый токен в редакторе

### Сохранение токенов при миграции

При импорте из Keitaro:
1. Токены из Keitaro автоматически переносятся в Orbitra
2. Сохраняется полная совместимость с существующими интеграциями
3. Интеграционные скрипты продолжат работать без изменений

### Где используется токен

- **Интеграционные скрипты** на лендингах
- **API запросы** от сторонних систем
- **Постбеки** от партнерских сетей
- **Вебхуки** от внешних сервисов

## 🔄 Миграция из Keitaro

### Подготовка дампа Keitaro

На сервере Keitaro выполните:

```bash
# 1. Подключитесь к Keitaro серверу
ssh root@YOUR_KEITARO_SERVER_IP

# 2. Создайте дамп настроек (без логов кликов)
bash -lc '
source /etc/keitaro/env/inventory.env

# Конфиг для подключения к БД
cat > /root/keitaro-mariadb.cnf <<EOF
[client]
user=$MARIADB_KEITARO_USER
password=$MARIADB_KEITARO_PASSWORD
host=127.0.0.1
port=3306
protocol=tcp
EOF
chmod 600 /root/keitaro-mariadb.cnf

# Создание дампа таблиц
TABLES="keitaro_affiliate_networks keitaro_groups keitaro_offers keitaro_domains keitaro_campaigns keitaro_campaign_postbacks keitaro_landings keitaro_streams keitaro_stream_filters keitaro_stream_offer_associations keitaro_stream_landing_associations keitaro_traffic_sources keitaro_ref_sources"

mysqldump --defaults-extra-file=/root/keitaro-mariadb.cnf \
  --single-transaction --quick --skip-lock-tables \
  "$MARIADB_KEITARO_DATABASE" $TABLES \
  | gzip > /root/keitaro_orbitra_full.sql.gz

ls -lah /root/keitaro_orbitra_full.sql.gz
'

# 3. Скачайте файл к себе
scp root@YOUR_KEITARO_SERVER_IP:/root/keitaro_orbitra_full.sql.gz .
```

### Импорт в Orbitra

1. Откройте **Миграции** в меню админки
2. Следуйте инструкции в блоке "Как создать бекап Keitaro"
3. Загрузите файл `keitaro_orbitra_full.sql.gz`
4. Выберите что импортировать:
   - ☑ Партнёрки (affiliate networks)
   - ☑ Офферы
   - ☑ Домены
   - ☑ Кампании
   - ☑ Потоки (streams)
   - ☑ Постбеки кампаний
   - ☑ Лендинги
   - ☑ Источники трафика
5. Нажмите **"Показать предпросмотр"** для проверки
6. Нажмите **"Импортировать в Orbitra"** для реального импорта

### Что импортируется

| Сущность Keitaro | Таблица Orbitra | Особенности |
|------------------|-----------------|-------------|
| `keitaro_affiliate_networks` | `affiliate_networks` | Сохраняются postback URL |
| `keitaro_offers` | `offers` | Сохраняются URL, payout, geo |
| `keitaro_domains` | `domains` | Сохраняется парковка, HTTPS |
| `keitaro_campaigns` | `campaigns` | **Сохраняются токены!** |
| `keitaro_streams` | `streams` | Сохраняются фильтры, схемы |
| `keitaro_landings` | `landings` | Сохраняются названия |
| `keitaro_traffic_sources` | `traffic_sources` | Сохраняются параметры |
| `keitaro_ref_sources` | — | Используется в streams |

### Особенности миграции

- **Токены сохраняются** — ваши скрипты продолжат работать
- **Streams filters** — фильтры конвертируются в новый формат
- **Postbacks** — S2S постбеки переносятся автоматически
- **Preview mode** — предварительный просмотр перед импортом

## 📊 Дашборд и Аналитика

### Главный Dashboard

**4 секции в сетке 2x2:**
- **Топ Кампаний** — топ-10 по кликам
- **Топ Офферов** — топ-10 по конверсиям
- **Топ Лендингов** — топ-10 по конверсиям
- **Топ Источников** — топ-10 по кликам

**Фильтры:**
- Период (today, yesterday, 7d, 30d, this month)
- Автообновление (off, 30s, 1m, 5m)

### TrendsPage — детальная аналитика

**8 метрик с графиками:**
- 🔵 **Клики** — общее количество кликов
- 🟢 **Уник. клики** — уникальные клики
- 🟡 **Конверсии** — количество конверсий
- 🟣 **Доход** — общий доход (Revenue)
- 🔴 **Расход** — общие расходы (Cost)
- 🔵 **Прибыль** — Revenue - Cost
- 💗 **CTR** — Click Through Rate
- 🟢 **CR** — Conversion Rate

**Группировка:**
- По дням
- По неделям
- По месяцам
- По часам
- По дню недели

**Фильтры:**
- Кампания
- Оффер
- Источник
- Страна
- Устройство
- OS
- Браузер
- Параметры (keyword, sub_id, etc.)

### Campaign Reports

**Отчёты по кампаниям** с группировкой:
- **Группировка по**: Страна, Устройство, Язык, Stream, Source, Sub ID
- **Фильтр по периоду**: настраиваемый диапазон дат
- **Метрики**: клики, уники, конверсии, CR, EPC, Revenue, Real Revenue, Cost, Profit, Real ROI
- **Экспорт в CSV** — для анализа в Excel

## 🤖 Telegram Bot

### Возможности бота

**Команды:**
- `/stats` — статистика за сегодня
- `/stats 7d` — статистика за 7 дней (1d, 30d, yesterday)
- `/campaigns` — список активных кампаний
- `/campaign 123` — детальная статистика кампании
- `/top` — ТОП-5 кампаний по доходу 🥇🥈🥉
- `/conversions` — последние 10 конверсий
- `/notify on|off` — вкл/выкл уведомлений
- `/daily on|off` — вкл/выкл ежедневной сводки
- `/lang en|ru|uk|es|zh|fr|de` — язык бота
- `/help` — справка

### Уведомления

**Мгновенные уведомления:**
- При каждой конверсии
- Кампания, статус, сумма, валюта
- Страна с флагом
- Время конверсии

**Ежедневная сводка:**
- Cliks, конверсии, Revenue, Cost, Profit
- ТОП-3 кампании по доходу
- Настраиваемое время отправки

### Настройка бота

1. **Создайте бота** через `@BotFather` в Telegram
2. **Получите токен** бота
3. **Откройте** Интеграции → Telegram Bot
4. **Вставьте токен** → Подключить
5. **Отправьте** `/start` боту
6. **Готово!**

## 🎨 Мультиязычность (i18n)

### Поддерживаемые языки (7)
- 🇬🇧 **English (EN)**
- 🇷🇺 **Русский (RU)**
- 🇺🇦 **Українська (UK)**
- 🇪🇸 **Español (ES)**
- 🇨🇳 **中文 (ZH)**
- 🇫🇷 **Français (FR)**
- 🇩🇪 **Deutsch (DE)**

Все языки имеют 100% паритет ключей перевода.

### Переключение языка
- **Расположение**: Профиль → Настройки
- **Применение**: мгновенно без перезагрузки страницы
- **Сохранение**: per-user настройка

### Локализованные компоненты

Все 53 React компонентов полностью локализованы:
- Dashboard и аналитика
- CampaignEditor и все модальные окна
- Интеграции и настройки
- Уведомления и ошибки

## 🏪 Интеграции

### Партнерские сети (шаблоны)

**Готовые интеграции:**
- Leadbit
- M4Leads
- Dr.Cash
- Everad
- AdCombo
- LottoProfit
- Terraleads
- Unice

**Параметры по умолчанию:**
- `external_id` — внешний ID конверсии
- `payout` — сумма выплаты
- `currency` — валюта (USD, EUR, RUB)
- `country` — код страны
- `ip` — IP адрес
- `sub_id_1`...` — sub ID

### Источники трафика (шаблоны)

**Готовые интеграции:**
- Facebook Ads
- Google Ads
- TikTok Ads
- VK Ads
- Яндекс Директ

**Автоматические параметры:**
- Facebook: `fbclid`, `adset_id`, `campaign_id`, `ad_id`, `placement`
- Google: `gclid`, `campaign_id`, `adgroup_id`, `creative`, `target`
- TikTok: `ttclid`, `campaign_id`, `adgroup_id`, `ad_id`
- VK: `vk_click_id`, `vk_source_id`, `vk_campaign_id`

## 🔧 Дополнительные функции

### Управление доменами
- **DNS проверка** — автоматическая проверка A записи
- **HTTPS-only** — принудительный редирект на HTTPS
- **Защита от ботов** — `/robots.txt` и `X-Robots-Tag`
- **Parking mode** — парковка доменов

### Архив
- **Soft Delete** — удаление без потери данных
- **Восстановление** — восстановление из архива
- **Очистка** — окончательное удаление

### Профиль пользователя
- **Язык интерфейса** — 7 языков (EN, RU, UK, ES, ZH, FR, DE)
- **Часовой пояс** — полный список IANA
- **Первый день недели** — Понедельник/Воскресенье
- **Смена пароля** — обновление пароля

### Системные настройки
- **Брендинг** — логотип, цвета, название; темы Light, Dark, Green, Neon + кастомная палитра
- **Конверсии** — статусы и отображение
- **Метрики** — кастомные формулы
- **Анти-бот** — IP и User Agent черные списки + Bot Challenge (reCAPTCHA v2/v3 или свой код) для проверки человека по кампаниям

## 🚀 Симуляция трафика

### Тестирование потоков

**Настройка параметров клика:**
- **IP Address** — IP адрес клика
- **User Agent** — User Agent браузера
- **Страна** — страна клика (US, RU, DE, GB, FR, etc.)
- **Устройство** — desktop, mobile, tablet
- **Язык** — язык браузера (en, ru, de, fr, es, pt, zh)

### Результат симуляции

**Отображается:**
- ✅ Успех — поток сработал
- ❌ Ошибка — описание проблемы
- 📊 Trace — детальная информация

## ♻️ Очередь S2S-постбеков с retry (с версии 0.9.6.0)

До 0.9.6.0 исходящие S2S-постбеки отправлялись синхронно прямо во время обработки входящего постбека (`fire-and-forget`): если эндпоинт партнёрки был недоступен, постбек терялся безвозвратно. Теперь постбеки доставляются надёжно.

**Как работает:**
1. При входящем постбеке (`postback.php`) каждая исходящая доставка добавляется в таблицу `s2s_postbacks_log` со `status='pending'` и макро-подставленным URL.
2. Воркер `postback_queue_cron.php` (запускается по cron каждую минуту) выбирает «должные» строки, выполняет HTTP-запрос и пишет результат.
3. Критерий успеха — HTTP `2xx-399`. Иначе — повтор с экспоненциальным backoff: **60с → 5м → 30м → 2ч → 24ч**. Всего 6 попыток (1 первая + 5 повторов), после чего строка помечается `status='failed'` (видна в логах S2S).
4. SSRF-проверка выполняется дважды: при постановке в очередь и перед отправкой. Хост, который резолвится в приватный/зарезервированный адрес, помечается `failed` без повторов; неудачный DNS считается временной ошибкой и уходит в повтор.
5. Строку, взятую в работу (`status='in_flight'`) и брошенную упавшим воркером, следующий запуск возвращает в очередь через 10 минут — иначе она была бы невидима для выборки, которая смотрит только на `pending`.

> ⚠️ **Без установленного крона очередь не разбирается и постбеки не уходят.** Это первое, что нужно сделать после обновления на 0.9.6.0.

**Установка воркера:** **Настройки → Автоматизация → Очередь постбеков**, кнопка «Установить cron доставки» — действие `postback_queue_install_user_cron` (ставит строку `* * * * *` в crontab пользователя). Удаление — `postback_queue_remove_user_cron`. Состояние воркера (last ping, processed/delivered/requeued/failed за последний запуск, счётчики по статусам) отдаёт `postback_queue_info` и показывает та же панель.

**Расширенные макросы** (раньше было 6, теперь поддерживаются все): `{subid}`, `{status}`, `{payout}`, `{currency}`, `{external_id}`, `{tid}`, `{campaign_id}`, `{cost}`, `{revenue}`, `{profit}`, `{sub_id_1}` … `{sub_id_30}`.

**Исправленный баг POST:** постбеки с методом POST теперь отправляют тело (раньше уходил пустой POST). Поля из query-string переносятся в `CURLOPT_POSTFIELDS` с `Content-Type: application/x-www-form-urlencoded`.

## 💰 Автоимпорт расходов из Facebook Ads / Google Ads (с версии 0.9.6.0)

Два новых engine агрегатора забирают дневной рекламный расход и атрибутируют его к кликам, так что отчёты ROI/прибыли больше не показывают нулевой расход.

**Engines** (в `aggregator_engines/`):
- **FacebookAdsEngine** — Facebook Marketing API `/act_<id>/insights`. Авторизация: long-lived User Access Token или System User Token + Ad Account ID.
- **GoogleAdsEngine** — Google Ads API (REST, v19, GAQL). Авторизация server-to-server OAuth2: `developer_token` + `client_id` + `client_secret` + `refresh_token` + `customer_id`. `refresh_token` не протекает; короткоживущий `access_token` получается автоматически и кешируется в таблице `oauth_tokens`.

**Атрибуция к кликам:** импортированный расход (таблица `cost_records`) распределяется по кликам через `UPDATE clicks SET cost = cost + ?`, группируя по дню и рекламному ID. Ключи join — `json_extract(clicks.parameters_json, '$.ad_id')` → `$.campaign_id` → `$.campaignid` (для ValueTrack), те самые ID, которые уже проставляются встроенными traffic-source-шаблонами Facebook и Google Ads.

**Настройка:** Администрирование → Агрегатор → новый connection с engine `facebook` или `google_ads`, поля форм генерируются динамически через `getRequiredFields()`. Тест подключения — кнопкой «Test Connection». Синхронизация — по cron (`aggregator_cron.php`) или вручную (кнопка Sync).

---

*Функции обновлены для версии v1.3.9*
